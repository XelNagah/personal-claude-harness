# SO / editor
Thumbs.db
Desktop.ini
.DS_Store
*~
.vscode/
.idea/

# Config local de Claude Code en este repo (no se distribuye)
.claude/settings.local.json

# Scratch / temporales de trabajo (handoffs, notas de sesión)
.claude/tmp/

# Respaldo que deja el nivelador antes de pisar lo Base: copia completa de .claude/,
# de un solo uso. Se borra una vez verificado que el nivelado quedó bien.
.claude/.respaldo-amp/

# Adjuntos que deja una sesión remota de Codex (imágenes que se le pasaron al agente).
# No son del repo: los escribe el cliente al costado del checkout.
.codex-remote-attachments/
