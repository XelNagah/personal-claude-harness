---
indice: Índice de la base de conocimiento
origen: agente-desplegado
columnas: [Código, Nombre, Descripción, Detalle]
descripcion: de qué trata esa página, en una línea
---

# Índice de la base de conocimiento

Índice raíz de lo que el agente **sabe** sobre este proyecto. Solo punteros — una fila por página o sección, nunca contenido.

Los markdown de la raíz del repo (README y REGISTRO) son **documentación del proyecto**, no conocimiento de agente: no se listan acá.

- **Código** — `Local-NNNN`. Se asigna al crear la entrada y no se reusa.
- **Nombre** — el título de la página.
- **Descripción** — de qué trata, en una línea: lo suficiente para decidir si vale abrirla.
- **Detalle** — la página, o la carpeta con su propio índice.

Convención completa en el [README del subsistema](README.md).

## Páginas

| Código | Nombre | Descripción | Detalle |
|---|---|---|---|
| Local-0001 | Modos de falla ante reglas escritas | Cinco formas distintas en que un agente incumple una regla que tiene cargada (recita sin obedecer, negocia, deja las cosas afuera, pide permiso para escribir pero no para ubicar, se inventa reglas); el texto de arranque gobierna la conversación, no la acción. | [modos-de-falla-ante-reglas-escritas.md](modos-de-falla-ante-reglas-escritas.md) |
| Local-0002 | grep y acentos en Windows | `grep -i` con patrones acentuados devuelve 0 resultados en silencio en Git Bash; enumerar variantes sin `-i`. | [grep-y-acentos-en-windows.md](grep-y-acentos-en-windows.md) |
| Local-0003 | Hooks de Claude Code — referencia de mecánica | Cómo funcionan los hooks: los 9 eventos del núcleo, cuáles bloquean, el contrato exit-code/JSON, `additionalContext` (al modelo) vs `systemMessage` (al usuario), matchers, precedencia y config; `PreToolUse` **sí inyecta `additionalContext`** (junto al resultado de la tool) además de allow/deny/ask/defer; paridad Codex limitada a UserPromptSubmit/SessionStart. **Corregido 29/07/2026:** `CLAUDE_PROJECT_DIR` **no** es la raíz del repo sino el directorio de lanzamiento, y §5.6 trae la forma que resuelve la raíz sin romper ni irse al repo vecino. | [hooks-claude-code.md](hooks-claude-code.md) |
| Local-0004 | Hooks de Codex CLI — cobertura, formato y límites | Verificado 26/07/2026: `apply_patch` **sí** dispara `PreToolUse`/`PostToolUse` y matchea como `apply_patch`, `Edit` o `Write` (el "solo Bash" quedó viejo en abril), pero los datos llegan en `tool_input.command`, no en `file_path`; el `deny` **no frena la escritura** (bug abierto) y un hook no corre hasta que se le da confianza a mano, confianza que se pierde cada vez que cambia su texto. **Caduca** — dos de los tres límites son bugs abiertos. | [hooks-codex-cli.md](hooks-codex-cli.md) |
| Local-0005 | Latencia de los hooks de Claude Code | Qué cuesta cada mecanismo (números medidos): el costo dominante es arrancar el intérprete (~50 ms Node, ~30 ms `cmd`, ~140 ms PowerShell), no la lógica; presupuesto <100 ms por evento bloqueante; `UserPromptSubmit` se paga en cada mensaje, `PreToolUse` es imperceptible; el episodio de los 5 s fue un hook colgado pagando su timeout. | [latencia-hooks.md](latencia-hooks.md) |
| Local-0006 | Proyectos similares al harness | Relevamiento 2026-07-21/23 del ecosistema (Agent OS, bancos de memoria, BMAD/SuperClaude, Hermes Agent de Nous Research): nadie combina lo mismo, el espacio está libre; diferenciadores del harness y riesgo de que lo lean como "otro banco de memoria". **Caduca** — el ecosistema se mueve rápido. | [proyectos-similares-al-harness.md](proyectos-similares-al-harness.md) |
| Local-0007 | Replicar los componentes de Hermes en el AMP | Mapeo de los cinco componentes de Hermes (memoria, skills, persona, tareas agendadas, bucle de auto-mejora) a los subsistemas del Agente Multipropósito: qué está cubierto, qué falta, y los cuatro huecos sin plan (búsqueda de sesiones pasadas, auto-escritura de memoria, corte por seguridad de costo, auto-escritura de skills). | [replicar-hermes-en-el-amp.md](replicar-hermes-en-el-amp.md) |
| Local-0008 | El repo que un script describe | Un script que inspecciona "el repo" debe tomarlo del directorio de trabajo, nunca de su propia ubicación: en cuanto existe una segunda copia (plugin, marketplace bajado, repo consumidor) deducirlo desde `__dirname` describe y modifica el repo equivocado, y no falla — contesta. Incluye el mismo error un nivel más arriba (29/07/2026): arreglar **dónde está** el script no arregla **qué repo mira**. | [el-repo-que-un-script-describe.md](el-repo-que-un-script-describe.md) |
| Local-0009 | Despliegue de plugins y migraciones | Paradas y **cuatro** desfases de una versión (el cuarto: la dependencia que el repo nunca declaró ⇒ el plugin no carga y solo lo dice el registro de depuración); mecánicas del CLI que sorprenden, incluido que `update` no repara dependencias y que `install` repara una por corrida; diferencias entre Codex y Claude Code; prueba sobre un consumidor viejo; cierre por hash remoto, registros activos, control completo y agente recién iniciado. | [despliegue-de-plugins-claude-code.md](despliegue-de-plugins-claude-code.md) |
| Local-0010 | Terminología farlopa: la deriva terminológica de los agentes | Los agentes incorporan términos ajenos (anglicismos, copias literales del inglés, jerga) al dominio sesión tras sesión sin ratificar; fenómeno universal a todo repo con agentes, síntoma la perplejidad del autor, y origen del subsistema semántica. | [terminologia-farlopa.md](terminologia-farlopa.md) |
| Local-0011 | Terminología canónica | Una regla escrita con el vocabulario que prohíbe se auto-refuerza; ratificar un término no alcanza hasta bajarlo a todo el texto normativo. | [terminologia-canonica.md](terminologia-canonica.md) |
| Local-0012 | Cambiar la forma de un registro rompe a sus lectores | Al cambiarle las columnas a un registro, el código que lo lee por posición o por el texto del encabezado pasa a validar sobre un conjunto vacío y **contesta en verde**: de once roturas medidas, ocho no emitieron ninguna señal. Qué hacer (ubicar por nombre, que la ausencia sea error y no cero, una prueba por control con caso bueno y malo) y tres trampas de parseo verificadas: tuberías escapadas, `decodeURIComponent` ante un `%` suelto, y el encabezado compartido entre archivos concatenados. | [cambiar-la-forma-de-un-registro.md](cambiar-la-forma-de-un-registro.md) |
| Local-0013 | Controles que dejan de controlar sin avisar | Las cinco formas en que un control se apaga solo y sigue dando verde: valida sobre un conjunto vacío, marca tanto que se lo deja de leer (una fila que marcaba 37 y acertaba 0, con 3 errores reales escondidos entre 89 que no lo eran), mira una copia y no la que se usa, nadie lo probó nunca, o tiene adentro una condición que no controla nada (2 de 5 condiciones nuevas, con el banco verde con ellas y sin ellas). Incluye la tensión entre las dos primeras —el remedio de marcar de más produce el mudo— y el hallazgo irresoluble, que apaga el reporte aunque sea uno solo. Cómo se prueba un control —caso bueno y malo, romper cada condición por separado, sin números absolutos adentro, banco aparte— y por qué reportar y fallar son contratos distintos. | [controles-que-no-avisan.md](controles-que-no-avisan.md) |
| Local-0014 | El mismo dato en dos lugares, sin nada que los sincronice | Un dato escrito dos veces diverge siempre; lo que decide el daño es si hay un control que compare. Cuatro formas medidas: el texto que se publica duplicado del que se usa (2 de 12 scripts divergentes con el control en verde), el comentario que sobrevive al código y afirma lo contrario, el número que copia un dato que cambia —y la distinción que lo salva: contar el pasado no envejece, afirmar el presente sí, y la referencia por posición es el peor caso porque apunta en vez de contar—, y el dato que un archivo declara y otro repite. Incluye las dos trampas del texto que viaja —controles asimétricos que empujan a divergencia, e historia del autor que no debe viajar— y el trabajo terminado que muere en la carpeta temporal. | [el-mismo-dato-en-dos-lugares.md](el-mismo-dato-en-dos-lugares.md) |
| Local-0015 | La marca de orden de bytes tapa el frontmatter | Un `.md` guardado con U+FEFF deja de matchear `^---` y pierde todo lo que declaraba de sí mismo, sin emitir señal: el archivo se ve igual en cualquier editor y "sin frontmatter" es una respuesta que el código ya sabe manejar. Qué cambia según quién lee (el que copia por `origen` publica las filas del repo; los lints leen un Índice declarado como no declarado), por qué el arreglo es de a trece a la vez, y la trampa de escribir el carácter literal en el código. | [marca-de-orden-de-bytes-y-frontmatter.md](marca-de-orden-de-bytes-y-frontmatter.md) |
