# Decisiones del proyecto

Registro de las decisiones **estructurales al propósito del repo**: las que definen cómo es o qué hace el repo en lo esencial, o que eligen un camino entre varios de forma que **condiciona el trabajo futuro**. **No** van las operativas triviales o efímeras ("busqué X en internet", "usé tal flag"). Ante la duda: ¿esto condiciona el repo a futuro? Sí → va.

Una fila por decisión:

- **N°** — secuencial (`0001`, `0002`, …), referencia estable.
- **Decisión** — qué se decidió y por qué, en una frase (para las simples).
- **Fecha** — `AAAA-MM-DD`.
- **Estado** — `vigente` o `reemplazada por NNNN`. Para revertir no se borra: se agrega una nueva y se marca la vieja.
- **Detalle** — link a `NNNN-nombre.md` **solo si la decisión requiere conceptualización mayor** (contexto, alternativas, consecuencias); `—` si es simple.

| N° | Decisión | Fecha | Estado | Detalle |
|----|----------|-------|--------|---------|
| 0001 | El repo es un **setup de funcionalidades para agentes de propósito general (multipropósito)**, no un inicializador de repos de software: el usuario define el propósito del repo y los subsistemas se llenan con lo aprendido para lograrlo (ej.: agente contable con gnucash, análisis de casas, prueba de modelos). Se puede instalar sobre un repo vacío **o sobre uno que ya tenga cosas** (idempotente/reconciliable). | 2026-07-18 | vigente | — |
| 0002 | **Patrón de subsistema:** un `INDICE.md` con **entradas**; cada entrada puede referenciar un **documento** de detalle (`.md`) o una **carpeta** con su propio índice (recursivo). Guardado por un lint de integridad. **Sin grafo explícito** (no aporta a este tamaño de repo). Síntesis propia, sin atribución externa. *(La cláusula original "el `INDICE.md` va siempre en contexto" fue **revisada por 0017**: siempre va la descripción del subsistema; el índice se carga solo si la descripción lo declara.)* | 2026-07-18 | vigente | — |
| 0003 | **Integridad en dos capas:** mecánica (lints `.js`, sin LLM: refs, huérfanos, índice, colisiones) **obligatoria para todo subsistema que persiste estado**; semántica (contradicciones, incompatibilidades, duplicación, desactualización — requiere LLM) hoy informal, pendiente de formalizar. Los operacionales (`setup-completo`, `planificar`) no llevan lint: no guardan estado. | 2026-07-18 | vigente | — |
| 0004 | **Gobernanza de terminología:** el agente no acuña términos del dominio por cuenta propia; prefiere las palabras del usuario. **Español corriente en todo**: nada de palabras inventadas o raras (aunque suenen técnicas), ni en texto plano ni en diagramas — no solo en los registros. Control duro en registros canónicos (glosario, decisiones): ningún término acuñado se asienta sin ratificación; en texto plano/diagramas se usa marcado como propuesto. (Motivada por dos inventos que se colaron: "sustrato" al glosario/decisión, y "acretar" a un diagrama del README.) | 2026-07-18 | vigente | — |
| 0005 | **Estados de planes en un solo eje** (`Nuevo · En curso · Diferido · Ejecutado · Descartado`): un plan tiene exactamente un estado a la vez. Declarados en `planes/ESTADOS.md`, **configurable por repo** (estado → sentido, carpeta, terminal); el `lint-planes` lo lee en vez de hardcodear. Elimina la columna `Prioridad` del registro (su info queda absorbida en el estado). Reemplaza el modelo previo de dos ejes (prioridad `foco`/`estacionado` × estado `idea`/`en diseño`/`listo`/`en ejecución`); los términos viejos se **barren, no se registran como alias**. Default en español. | 2026-07-18 | vigente | — |
| 0006 | **Nomenclatura en español para todos los subsistemas del harness:** renombrar `memory/`→`memoria/` y su índice `MEMORY.md`→`MEMORIA.md`, para que ningún subsistema quede en inglés. La auto-memoria del sistema Claude Code (fuera del repo, `~/.claude/projects/.../memory/`) queda como está: nombre fijo del harness oficial, fuera de nuestro control. | 2026-07-18 | vigente | — |
| 0007 | **El subsistema `scripts` se generaliza a `herramientas` (Tools):** catálogo de las *tools* que el Propósito del repo requiere. Tipos: `script`, `skill` local del repo, `MCP` local; hooks **afuera** (automatización disparada por evento, no invocable por el agente). Registro con columna **Tipo**; cada fila apunta a donde vive la tool (script en su carpeta bajo el subsistema, skill en `.claude/skills/`, MCP en `.mcp.json`). Nombre en español (0006). Motivación: "script" quedó corto para la categoría. | 2026-07-19 | vigente | — |
| 0008 | **Los lints se co-ubican con su subsistema, no centralizados:** cada subsistema es índice + entradas + **su lint** en su propia carpeta (`.claude/<sub>/lint-<sub>/`), alineando el disco con 0003 (el lint siempre fue parte del Patrón del subsistema). Disuelve `.claude/scripts/` como carpeta compartida y elimina el acoplamiento cruzado (cada funcionalidad instalaba su lint en territorio de `scripts`). `lint-harness` (transversal, no es de un subsistema) es Herramienta de dominio de **este** repo (autorar el harness) y vive en `.claude/herramientas/lint-harness/`. | 2026-07-19 | vigente | — |
| 0009 | **Taxonomía de skills del harness:** las skills se clasifican por **ámbito**: *Skill de Subsistema* (opera sobre un subsistema concreto, viaja en el plugin de su funcionalidad — incluye las de prácticas como cerrar un plan) y *Skill del Agente Multipropósito* (transversal, sin subsistema dueño, funcionalidad propia — hoy `planificar`). Las de autoría del harness no son una clase aparte: son Herramientas del Propósito de este repo (0007), locales y no distribuibles. Criterio skill vs script: lo mecánico determinista va en script/lint; lo que requiere juicio o significado va en skill (que puede llevar scripts auxiliares). | 2026-07-19 | vigente | — |
| 0010 | **Arquitectura multiagente del harness:** `AGENTS.md` en la raíz como fuente única de instrucciones (estándar de la Linux Foundation, leído nativo por Codex/Cursor/Gemini/Copilot) con `CLAUDE.md` reducido a adaptador de una línea (`@AGENTS.md`); los subsistemas quedan en `.claude/` como casa de datos única para todos los agentes; `SKILL.md` (estándar abierto Agent Skills) pasa a formato único y se retiran los `prompt.md`; skills distribuidas a nivel usuario con junctions dobles (`~/.claude/skills/` + `~/.agents/skills/`); hooks con registro doble (settings.json de Claude + hooks de Codex, mismo script). Paridad de comportamiento objetivo: Claude Code ↔ Codex CLI. | 2026-07-19 | vigente | [0010-arquitectura-multiagente.md](0010-arquitectura-multiagente.md) |
| 0012 | **La Pantalla de bienvenida se emite vía `systemMessage`, no `additionalContext`:** un SessionStart hook solo pinta la terminal del **usuario** con el campo `systemMessage`; `additionalContext` (lo que se usaba) llega **solo al modelo**, invisible al usuario — por eso no se veía nada. Es cross-agente: Claude Code (CLI), Codex y Gemini soportan `systemMessage` al arrancar; **Cursor no** tiene banner nativo (degrada sin caja, sin envoltorio de launcher). La caja es de **ancho automático** (se dimensiona al renglón más largo) para no desarmarse cuando una métrica gana dígitos. (Corrige una premisa falsa asentada el 26-07-20: que `additionalContext` era "full-visible en el transcript".) | 2026-07-21 | vigente | — |
| 0013 | **Segmentación de skills por prefijo de plugin:** las skills del harness se diferencian por subsistema con el prefijo nativo `plugin:skill` de Claude Code (`memoria:registrar-memoria`, `glosario:converger-terminologia`) — el prefijo **es** el nombre del plugin. **No** por renombre de skills ni por un prefijo único `amp:` (un único prefijo exige un mega-plugin que esconde el subsistema y rompe la instalación modular 0009). Exige que el prefijo sea el subsistema pelado: se limpian los nombres de plugin (`memoria-local`→`memoria`, `gestion-de-planes`→`planes`, `preferencias-trabajo`→`preferencias`, `estilo-commits`→`commits`; glosario/decisiones/conocimiento/herramientas ya están). La **máquina de autoría pasa de junction a instalación por plugin** (marketplace local) para que la segmentación sea visible, aceptando perder la edición en vivo (el plugin copia a cache). Codex/Cursor/Gemini quedan **diferidos**: instalan por junction (Agent Skills), sin mecanismo de prefijo ⇒ la segmentación es hoy Claude-Code-only y rompe la paridad 0010 a propósito. *(La cláusula del **prefijo pelado** fue **modificada por 0029**: el prefijo pasa a `amp-<sub>:`; el multi-plugin por subsistema y la segmentación por prefijo siguen vigentes.)* | 2026-07-21 | vigente | — |
| 0011 | **Responsabilidad del índice en el Patrón recursivo (0002):** cada índice responde por las páginas de su directorio y de los subdirectorios **sin índice propio**; un subdirectorio con su propio `INDICE.md` (una **Carpeta**, en términos del glosario) es **una sola Entrada** para el índice de arriba — se lo menciona una vez, no sus páginas internas. Un sub-índice se atribuye a su ancestro **estricto** más cercano, así el padre queda obligado a nombrarlo. Regla para el lint de cualquier subsistema: cada página se atribuye a su índice ancestro más cercano; el fallback por carpeta recorre la cadena de ancestros hasta el directorio del índice, no solo el padre inmediato; un hallazgo cae una sola vez y contra el índice que corresponde. (Motivada por un hueco de `lint-conocimiento` que hacía responsable al índice raíz por páginas ya delegadas a un sub-índice.) | 2026-07-20 | vigente | — |
| 0015 | **Nomenclatura de skills y Herramientas en verbo+objeto:** el nombre es una acción (verbo + objeto: `registrar-memoria`, `inventariar-componentes-sueltos`, `instalar-junctions`), no un adjetivo ni una frase nominal (`fuera-de-subsistema` ✗). Excepción: los lints conservan `lint-<subsistema>` (nombre fijo del Patrón, decisión 0008). (Motivada al nombrar `inventariar-componentes-sueltos`, corrigiendo la propuesta adjetiva `fuera-de-subsistema`.) | 2026-07-21 | vigente | — |
| 0016 | **Los nombres de skills y Herramientas requieren ratificación del usuario**, igual que los términos canónicos (decisión 0004): el agente propone, no bautiza; ningún nombre se fija sin el ok explícito. Un nombre de tool es una interfaz estable que viaja a los repos consumidores. | 2026-07-21 | vigente | — |
| 0017 | **Modelo de carga de contexto: descripción por subsistema siempre cargada; índice bajo declaración.** Lo que va siempre en contexto es una **descripción liviana por subsistema** (qué es, cómo se usa, cuándo consultarlo), no su índice. Cada descripción **declara si su índice también se carga** incluyendo —o no— la línea `@INDICE.md`: import **anidado** de Claude Code (cadena `CLAUDE.md→AGENTS.md→descripción→índice`, hop ≤3, bajo el límite documentado de 4). Como **no existe import condicional**, la presencia de la línea **es** la declaración y no puede divergir de la realidad (sin lint que la vigile). Se descartaron **M2** (AGENTS.md arma el import a mano honrando un flag → el flag puede mentir, exige lint) y **M3** (hook ensambla en runtime vía `additionalContext` → máquina extra, canal solo-modelo). **Revisa la cláusula "el `INDICE.md` va siempre en contexto" de 0002** (el resto de 0002 sigue vigente). Fuera de Claude Code (Codex/Cursor/Gemini no expanden imports) degrada a instrucción de lectura, igual que 0010. Motiva: `PLANES.md` era el índice más pesado (~21k chars, casi la mitad del contexto siempre cargado; la mitad de sus filas eran terminales históricas). | 2026-07-21 | vigente | — |
| 0018 | **Modelo de términos por estado en el glosario + veto (extiende 0004):** cada concepto agrupa sus términos en un solo eje de estado — `canónico` (la celda Concepto), `alias` (válido, ratificado), `propuesto` (sugerido por el agente, sin usar hasta ratificar) y `vetado` (prohibido; el reemplazo es el canónico de su propia fila). La tabla pasa a 6 columnas (`Concepto \| Definición \| Alias \| Propuestos \| Vetados \| Detalle`). **Gobernanza:** el agente nunca escribe en `Alias` ni en `Vetados` —solo propone en `Propuestos`—; ratificar y vetar son del usuario; el agente nunca usa un término propuesto o vetado. Extiende 0004 (que cubría conceptos) a los **alias**, y agrega **prohibir**, no solo registrar. Los vetados sin concepto de dominio propio (anglicismos, calcos, jerga) se agrupan bajo el concepto **Terminología Farlopa**, con su mapa término→reemplazo en la página de Detalle (fenómeno universal, en el conocimiento `terminologia-farlopa`). El `lint-glosario` gana: contradicción alias/vetado, vetado ambiguo (mismo término en dos filas → pedir Detalle), propuestos pendientes (recordatorio) y apariciones de vetados en el repo (dos grupos: texto plano accionable / código informativo; autoexcluye glosario y planes ejecutados/descartados). Motivada: registrar un alias lo legitima ⇒ un término confuso queda como válido y el agente lo reusa. | 2026-07-21 | vigente | — |
| 0019 | **Cableado y estructura del Manifiesto de subsistema (extiende 0017):** los manifiestos se importan **directos** en una sección `## Subsistemas` de `AGENTS.md` (0010), **no** vía un archivo agregador — un agregador suma un hop (3→4, al ras del límite documentado de 4, margen 0) y duplica la indirección para los agentes que no expanden imports (Codex/Cursor/Gemini); solo se justificaría si llevara **contenido propio** (autodescripción del Agente Multipropósito completo), no como mero cargador. **Estructura obligatoria** de cada `MANIFIESTO.md`: 5 campos — (1) título, (2) identidad (qué es + dónde vive), (3) **disparador** (cuándo se consulta y cuándo se escribe), (4) declaración explícita de carga del índice (`se carga siempre` / `NO se carga siempre` + cuándo consultarlo a demanda), (5) lint al cerrar — más la línea `@INDICE.md` presente **sólo si** el índice se carga (su presencia ES la declaración, M1 de 0017). **Sin frontmatter** (un flag de carga resucitaría el M2 descartado por 0017). El chequeo de que el manifiesto tenga los campos vive en `lint-harness` (lado autor, junto al chequeo de tamaño de 0017), **no viaja** al consumidor (se instala correcto desde PLANTILLA). | 2026-07-22 | vigente | — |
| 0020 | **Test de demarcación: dónde va cada Componente se decide por 5 ejes** — de quién es (Producto del Propósito ↔ harness) · qué relación tiene el agente con la cosa (la lee ↔ la sabe ↔ la ejecuta) · cambia o está fijo (estado vivo ↔ veredicto) · es invocable por sí mismo (fila del registro ↔ contenido interno) · sustantivo o verbo (glosario ↔ Herramienta/comando). Los ejes son el test; los casos resueltos y los criterios finos viven en el detalle. Materializa el frente A del plan *efecto conductual*: baja a un registro consultable las reglas de demarcación que se acordaban y se evaporaban. | 2026-07-22 | vigente | [0020-test-de-demarcacion.md](0020-test-de-demarcacion.md) |
| 0021 | **Subsistema `conducta`: reglas "cuando hagas X, asegurate de Y" atadas a momentos del flujo.** Ata **momentos** (evento de hook + condición sin juicio) a **acciones** (inyectar texto / correr una Herramienta / bloquear), entregadas por un hook repartidor por evento que lee un registro compartido. Agente-agnóstico: cada agente declara con qué mecanismo realiza cada momento (Claude por `PreToolUse`; Codex desviando por Bash; o "no realizable", documentado). Viene con una **Base** instalada (respetar preferencias, considerar conocimiento, registrar cuando algo cambia) + reglas del Propósito de cada repo. Se apoya en los otros subsistemas, no los duplica. Descarta la config fija como solución única (agregar reglas a mano sin lint es la conducta que el harness combate). Absorbe los planes de control en el punto de acción. | 2026-07-22 | vigente | [0021-subsistema-conducta.md](0021-subsistema-conducta.md) |
| 0022 | **Autodescripción del mecanismo de los subsistemas = una skill transversal que ensambla, no docs nuevas.** El pedido "que el agente explique cómo funciona cada subsistema y el AMP completo" se resuelve con **una** Skill del Agente Multipropósito (0009) —`explicar-subsistema <sub>`, verbo+objeto (0015), nombre provisional a ratificar (0016)—, empaquetada como funcionalidad propia (como `planificar`; operacional ⇒ sin lint, 0003). **Ensambla** la respuesta desde las fuentes ya instaladas bajo `.claude/<sub>/` —`MANIFIESTO.md` (qué es + disparador), las skills del subsistema (skills + flujo, que el modelo ya tiene en su catálogo de skills), `amp-info` (estado), y `feedback_<sub>` para el orden multi-paso— narrando a profundidad variable por juicio (default media; profundiza a demanda). Sin argumento explica la articulación del AMP completo (fuente `AGENTS.md` + decisiones). **Descarta** una skill por subsistema (8 copias del mismo flujo a sincronizar) y la sección en `INDICE` (engorda el contexto siempre-cargado, contra 0017; nada obliga a contestar así). No se escribe doc consolidada nueva: la dispersión en 4 lugares (SKILL, feedback, README de plugin, AGENTS.md) se resuelve **designando la fuente canónica por faceta**, no agregando una quinta. La mitad "en qué estado está" ya la cubre `amp-info`; el bloqueo de nomenclatura no aplica (los nombres de subsistema son firmes). | 2026-07-23 | reemplazada por 0023 | — |
| 0023 | **Autodescripción vía MANIFIESTO enriquecido (reemplaza 0022, extiende 0019).** El pedido "que el agente explique cómo funciona cada subsistema" **no** se resuelve con una skill nueva que ensambla en runtime (0022, descartada por sobreingeniería), sino enriqueciendo el propio `MANIFIESTO.md` —siempre cargado— para que **sea** la fuente. Se le suman dos campos: **Skills** (obligatorio — nombra las skills de **operación** del subsistema y qué hace cada una; la de **instalación** `inicializar-<sub>` se menciona al pasar; `conducta` pone "ninguna aún") y **Flujo de trabajo** (opcional — solo si es multi-paso, y como **puntero** al detalle on-demand en `feedback`/`SKILL`, nunca el paso a paso inline, para no engordar el contexto de arranque, 0017). Preguntar "¿cómo funciona X?" se contesta desde el MANIFIESTO ya en contexto + `amp-info` (estado), sin pieza nueva. **Anatomía del MANIFIESTO:** obligatorios — Título · Identidad · Disparador · **Skills** · Carga del índice · Lint; opcionales — Flujo · Gobernanza · Advertencias · Prueba de pertenencia; **una sola clase**, núcleo obligatorio + bloques opcionales que se prenden según el subsistema. El chequeo de campos vive en `lint-harness` (lado autor, no viaja). El **ordenamiento de los skills** en sí (ejecutar el prefijo 0013, renombrar los opacos, cerrar huecos de cobertura) ya vive en 3 planes abiertos (nomenclatura, prefijo/distribución, cobertura por subsistema), no necesita uno nuevo. | 2026-07-23 | vigente | — |
| 0014 | **Nombre del proyecto: "Agentes Multipropósito" (AMP) / "Multipurpose Agents" (MPA)** — en **una sola palabra**, la misma forma que el adjetivo (`el setup estándar multipropósito`). En singular, *Agente Multipropósito* es la línea de marca constante en todo repo consumidor. **Se evaluó y se descartó** la variante en dos palabras (*Agentes Multi Propósito*), que había quedado anotada como decidida el 20/07/2026: obligaba a sostener para siempre dos formas — el nombre separado y el adjetivo junto, porque separar el adjetivo rompe el castellano — y esa distinción era justamente lo que hacía reaparecer la discusión sesión tras sesión. Con una sola palabra no queda nada que distinguir: el glosario lleva **una fila única** con `AMP`/`MPA` como alias. **Se asienta acá porque hasta hoy el nombre vivía solo en notas de planes, que nadie consulta al arrancar** — por eso volvía. Pendiente aparte, por ser ruptura para los consumidores ya instalados: el nombre del marketplace (`xelnagah-harness`). | 2026-07-21 | vigente | — |
| 0024 | **El texto distribuido a un consumidor no expone el registro de decisiones del harness.** Los componentes que un `inicializar-*` escribe en el repo destino —lints `.js`, `MANIFIESTO.md` (los 6), docs de gobernanza (`glosario/INDICE.md`, `feedback_glosario.md`), bloques que se agregan a `AGENTS.md`, las PLANTILLAs y sus gemelos Textual en el orquestador— enuncian la regla o la razón **inline, sin citar el número** de decisión del harness. **Regla operativa:** un número de decisión solo aparece en archivos que **nunca** se instalan en un consumidor (`SKILL.md`/`README.md` de instalación, páginas de detalle, planes, prosa propia de `AGENTS.md`). Motivo: el número referencia el registro de **este** repo, que no viaja; en el consumidor colgaba (10+ refs sobre 6 decisiones citadas: 0004/0008/0010/0011/0017/0019, esta última en cada MANIFIESTO). **Camino 2 (sacar el número)** sobre Camino 1 (instalar las decisiones-base en el consumidor): evita ensuciar el registro del consumidor con arquitectura interna del harness (choca con 0001) y **disuelve** la ambigüedad de numeración (el registro del consumidor arranca en 0001) en vez de administrarla con rango reservado. `lint-harness` gana una guarda que falla si un archivo distribuible cita `decisión NNNN`. Cruza con `Separar mecánica del harness de criterio del autor`. | 2026-07-23 | vigente | — |
| 0025 | **Control de Terminología Farlopa en dos niveles según persistencia, dentro de `conducta` (extiende 0021/0018).** La fricción se pone donde el daño se acumula. **Conversación** (`cada turno`, `inyectar`): recordatorio soft del criterio de demarcación; el agente decide, sin frenar ni ruido extra — minimiza la aparición y **acepta fugas** (la conversación es efímera, bajo daño). **Persistencia** (`al escribir`), dos velocidades porque el daño real es que la TF se replica y reaparece desde los archivos: (a) **vetado conocido → `bloquear`** — `PreToolUse` corre `lint-glosario` sobre el contenido y deniega la escritura (programa, no se recita, falso positivo imposible); (b) **TF nueva → `inyectar` fuerte** — recordatorio que fuerza aplicar el criterio antes de persistir (juicio; no se puede frenar limpio porque no está en ninguna lista). **A demanda:** `converger-terminologia` (skill) barre lo persistido y engorda vetados; queda **fuera** del disparo automático (un hook no corre skills, y un skill que el agente elige invocar recae en el modo 1 "recita sin obedece"). **El criterio es relativo al usuario del Propósito del repo, no al autor** (un dev entiende `hook`/`deploy`; el usuario de un repo contable no) ⇒ el control viaja al consumidor y se recalibra por Propósito, separando criterio de autor de mecánica. Fundamento: conocimiento `modos-de-falla-ante-reglas-escritas` (lo mecánico como programa; lo de juicio re-inyectado cerca del momento). Materializa el frente C de *Que el harness tenga efecto conductual* para la clase terminología; los 3 planes de punto-de-acción consumen los mismos momentos con distinto contenido, sin fusionarse. | 2026-07-23 | vigente | — |
| 0026 | **Subsistema `semántica` (rebautiza `glosario`), con dos registros pares (generaliza como 0007; reemplaza la disposición de Terminología Farlopa de 0018; se apoya en 0025).** El subsistema `glosario` se generaliza y **renombra a `semántica`** —análisis de referencia de los términos del dominio, que mantiene la coherencia semántica del negocio en el tiempo—, igual que `scripts`→`herramientas` (0007) al quedar corto el nombre. Sostiene **dos registros pares** (Forma A): `GLOSARIO.md` (terminología legítima, término→significado) y `TERMINOLOGIA-FARLOPA.md` (relaciones vetadas), bajo un `lint-semantica` y un `MANIFIESTO`; ninguno se carga en contexto siempre. **Terminología Farlopa deja de ser una fila del glosario con mapa colgado y pasa a registro propio de relaciones vetadas**, columnas `Término \| Significado vetado \| Cómo decirlo`. **Lo vetado es la relación término→significado**, no el término ni el significado sueltos: el mismo término con otro significado puede ser legítimo (`plomería`=cañerías en un repo de fontanería, válido; `plomería`=mecánica interna de un subsistema, vetado). Es **relativo al lector del Propósito del repo** ⇒ el registro se calibra por repo, **sin columna de lector**. El **lint marca por término** (lo mecánico); el **agente juzga el significado** al leer la marca. **Canonizar un significado como concepto del glosario es una decisión independiente del veto** (default: no se canoniza — no se define cada palabra); el criterio vive en `Criterio de pertenencia al glosario`. Caso testigo `tripa`: **se queda** en Terminología Farlopa, no gana concepto propio. Grafo latente: Glosario y Terminología Farlopa son dos tipos de arista (`significa` / `vetado`) sobre el mismo grafo término↔significado (base de grafos = norte futuro, no se construye). EN del registro: *Farlop Terminology* (broma deliberada; marcada para que la publicación EN / `converger-terminologia` no la 'corrijan'). **El glosario pierde la columna `Vetados`: todo lo vetado —con canónico de reemplazo o sin él— es una relación término→significado y vive en `TERMINOLOGIA-FARLOPA.md`; el glosario queda en 5 columnas (`Concepto \| Definición \| Alias \| Propuestos \| Detalle`).** Se difieren: la arista de procedencia farlopa→concepto (parte del grafo latente) y una skill de resolución de términos `Propuestos → Alias/Farlopa` (hoy la resolución es a mano; el hueco cae en el plan de skills de operación por subsistema). Ejecución = plan de rebautizo del tamaño de `scripts`→`herramientas` (funcionalidad, plugin, orquestador, nivelador, `lint-glosario`→`lint-semantica`, `glosario:`→`semantica:` de la segmentación 0013). | 2026-07-23 | vigente | — |
| 0027 | **Separación por origen (Base vs aprendido) en los subsistemas, por granularidad — precondición del nivelador `amp-actualizar`.** El harness distingue lo que manda (**Base**, río arriba) de lo que el AMP aprende persiguiendo su Propósito (**aprendido**), para que el nivelador reemplace lo Base sin abrir lo del repo. Se realiza distinto según la granularidad del subsistema: **por fila** (conducta) → dos secciones con tabla propia (`## Reglas Base` / `## Reglas del Propósito`), molde de `PREFERENCIAS.md` (Base/Adaptaciones); **por archivo** (memoria) → los archivos Base tienen nombre propio y el nivelador los pisa por nombre, los aprendidos quedan intactos (su rediseño de fondo va aparte); **contenido puro aprendido** (glosario, semántica, conocimiento, decisiones, planes, herramientas) → el harness los instala vacíos y el nivelador de rutina **no los reescribe**; el mecanismo (lint, MANIFIESTO, estructura) es Base y se pisa entero. **No precluye** despachar contenido sembrado en glosario/semántica/conocimiento a futuro: sería una migración dedicada, no tarea del nivelador de rutina. Extiende el Patrón de subsistema (0002) con el origen como dimensión. | 2026-07-24 | vigente | — |
| 0028 | **Diseño del nivelador `amp-actualizar`: convergencia estructural sin estado, con respaldo y reporte (extiende 0001, se apoya en 0027).** Un solo skill pone al día el `.claude/` de un repo instalado contra la plantilla objetivo, **sin guardar número de versión**: converge por estructura (agrega lo ausente; aplica renombres conocidos —`glosario`→`semantica`— al detectar la forma vieja) en vez de correr una lista de migraciones versionadas (los repos viejos no tienen versión guardada; la vía versionada necesitaría igual un arranque estructural). **Base** (mecanismo del harness: lint, MANIFIESTO, estructura, cableado del hook) → **pisar sin preguntar, respaldando** la versión vieja antes (`.claude/.respaldo-amp/<fecha>/`) y reportando al final solo lo que cambió; el respaldo importa porque `.claude/` suele estar gitignoreado en el host (sin red de git). **Aprendido** (contenido del repo) → nunca se toca. **Reacomodo legacy** (primer contacto, archivos mezclados pre-0027) que pueda enredar contenido aprendido → **preguntar** antes (bloqueante). El respaldo es salida, no estado leído de vuelta ⇒ no rompe el sin-estado. Nombre a ratificar (0016). | 2026-07-24 | vigente | — |
| 0030 | **La Pantalla de bienvenida es una Regla Base del subsistema `conducta` (clase `correr`, momento `al arrancar la sesión`), no una Herramienta.** El bloque de estado que se emite al arrancar la sesión pasa a ser un comportamiento de Base de `conducta` ("al arrancar la sesión, mostrá la pantalla"): momento nuevo `al arrancar la sesión` (evento `SessionStart`, sin condición) en `MOMENTOS.md`; regla Base clase `correr` en `conducta/INDICE.md` cuyo Contenido es la ruta del script; el script se **co-ubica** con conducta (`.claude/conducta/mostrar-pantalla-bienvenida/`) y **sale** del registro de Herramientas; el repartidor `establecer-conducta` **implementa la clase `correr`** (ejecuta el script y reenvía su stdout verbatim — `{systemMessage}` en SessionStart) y se cablea a `SessionStart`. `amp:info` muestra la misma pantalla a demanda. **Extiende 0021** (primera realización de la clase `correr` + primer momento `SessionStart`), se apoya en **0012** (emisión por `systemMessage`) y **0008** (infra Base co-ubicada, fuera del registro de Herramientas). Motivo: distribuir la pantalla a **todos** los AMP por el mecanismo que ya viaja (conducta, dentro de `amp:inicializar`), en vez de un hook suelto + Herramienta que el orquestador no instalaba (hueco: un AMP consumidor no la recibía). | 2026-07-24 | vigente | — |
| 0029 | **Empaquetado del harness: un plugin transversal `amp` + un plugin `amp-<sub>` por cada subsistema con skill de operación (modifica 0013, refina 0009).** El harness se distribuye como marketplace con prefijo `amp-`: **`amp` transversal** (prefijo `amp:`, skills `inicializar` · `planificar` · `info` · `actualizar`) y un plugin por cada subsistema que tenga skill de operación —`amp-memoria`, `amp-conocimiento`, `amp-decisiones`, `amp-preferencias`, `amp-planes`, `amp-semantica`— con prefijo `amp-<sub>:` que **agrupa** al tipear "amp" **y** deja **visible** el subsistema (`amp-planes:ciclo-de-plan` ≠ `amp-conocimiento:registrar-conocimiento`; saber de qué subsistema es cada skill es el objetivo). Subsistema **sin** skill de operación (`herramientas`, `conducta`, `commits`) **no tiene plugin** hasta que gane una; su estructura la escribe `amp:inicializar`. **Bundle completo, no à la carte:** todo AMP instala los 6 `amp-<sub>` vía `dependencies` del plugin `amp` ⇒ **1 install/repo**, **project scope** (por repo, no user — el harness solo aplica a repos AMP; un repo sin harness no lleva estas skills). **Consolida los 10 `inicializar-<sub>` en un solo `amp:inicializar`** que escribe la estructura completa (refina 0009 y absorbe el orquestador `setup-completo`; sin argumento à la carte porque el bundle es siempre todo). **Modifica 0013:** el prefijo pasa de subsistema **pelado** (`memoria:`) a `amp-<sub>:`; el multi-plugin por subsistema y la segmentación por prefijo de 0013 **quedan**. Skills transversales renombradas (ratificadas): `inicializar-custom`→`amp:inicializar`, `amp-actualizar`→`amp:actualizar`, `amp-info`→`amp:info`, `planificar`→`amp:planificar`. **12 plugins → 7.** Absorbe los puntos 2/3/4 del plan `Modelo de distribución y empaquetado del harness`. Codex/Cursor/Gemini siguen diferidos por junction (el prefijo es mecanismo de Claude Code). Ejecución (migrar marketplace + carpetas + `dependencies` + renombres) pendiente como plan. | 2026-07-24 | vigente | — |
