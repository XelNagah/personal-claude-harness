---
indice: Decisiones del proyecto
origen: agente-desplegado
columnas: [Código, Nombre, Descripción, Fecha, Estado, Detalle]
descripcion: qué se decidió y por qué
---

# Decisiones del proyecto

Registro de las decisiones **estructurales al propósito del repo**: las que definen cómo es o qué hace el repo en lo esencial, o que eligen un camino entre varios de forma que **condiciona el trabajo futuro**. **No** van las operativas triviales o efímeras ("busqué X en internet", "usé tal flag"). Ante la duda: ¿esto condiciona el repo a futuro? Sí → va.

Una fila por decisión:

- **Código** — `Local-NNNN`, referencia estable. Se asigna al crear la entrada y no se reusa. En lo que queda escrito no va solo: se dice `Decisión Local-NNNN`.
- **Nombre** — de qué trata la decisión, en una frase corta. Único en el registro.
- **Descripción** — qué se decidió y por qué. Si requiere conceptualización mayor, la frase queda acá y el desarrollo va al `Detalle`.
- **Fecha** — `AAAA-MM-DD`.
- **Estado** — `vigente` o `reemplazada por NNNN`. Para revertir no se borra: se agrega una nueva y se marca la vieja.
- **Detalle** — link a `NNNN-nombre.md` **solo si la decisión requiere conceptualización mayor** (contexto, alternativas, consecuencias); `—` si es simple. El nombre del archivo conserva el número sin prefijo: no es el código, es un nombre corto legible.

Las filas van en **orden ascendente por Código**.

| Código | Nombre | Descripción | Fecha | Estado | Detalle |
|---|---|---|---|---|---|

| Local-0001 | El repo es un setup para agentes multipropósito | El repo es un **setup de funcionalidades para agentes de propósito general (multipropósito)**, no un inicializador de repos de software: el usuario define el propósito del repo y los subsistemas se llenan con lo aprendido para lograrlo (ej.: agente contable con gnucash, análisis de casas, prueba de modelos). Se puede instalar sobre un repo vacío **o sobre uno que ya tenga cosas** (idempotente/reconciliable). | 2026-07-18 | vigente | — |
| Local-0002 | Patrón de subsistema: índice, entradas y lint | **Patrón de subsistema:** un `INDICE.md` con **entradas**; cada entrada puede referenciar un **documento** de detalle (`.md`) o una **carpeta** con su propio índice (recursivo). Guardado por un lint de integridad. **Sin grafo explícito** (no aporta a este tamaño de repo). Síntesis propia, sin atribución externa. *(La cláusula original "el `INDICE.md` va siempre en contexto" fue **revisada por 0017**: siempre va la descripción del subsistema; el índice se carga solo si la descripción lo declara.)* | 2026-07-18 | vigente | — |
| Local-0003 | Integridad en dos capas: mecánica y semántica | **Integridad en dos capas:** mecánica (lints `.js`, sin LLM: refs, huérfanos, índice, colisiones) **obligatoria para todo subsistema que persiste estado**; semántica (contradicciones, incompatibilidades, duplicación, desactualización — requiere LLM) hoy informal, pendiente de formalizar. Los operacionales (`setup-completo`, `planificar`) no llevan lint: no guardan estado. | 2026-07-18 | vigente | — |
| Local-0004 | Gobernanza de terminología | **Gobernanza de terminología:** el agente no acuña términos del dominio por cuenta propia; prefiere las palabras del usuario. **Español corriente en todo**: nada de palabras inventadas o raras (aunque suenen técnicas), ni en texto plano ni en diagramas — no solo en los registros. Control duro en registros canónicos (glosario, decisiones): ningún término acuñado se asienta sin ratificación; en texto plano/diagramas se usa marcado como propuesto. (Motivada por dos inventos que se colaron: "sustrato" al glosario/decisión, y "acretar" a un diagrama del README.) | 2026-07-18 | vigente | — |
| Local-0005 | Estados de planes en un solo eje | **Estados de planes en un solo eje** (`Nuevo · En curso · Diferido · Ejecutado · Descartado`): un plan tiene exactamente un estado a la vez. Declarados en `planes/ESTADOS.md`, **configurable por repo** (estado → sentido, carpeta, terminal); el `lint-planes` lo lee en vez de hardcodear. Elimina la columna `Prioridad` del registro (su info queda absorbida en el estado). Reemplaza el modelo previo de dos ejes (prioridad `foco`/`estacionado` × estado `idea`/`en diseño`/`listo`/`en ejecución`); los términos viejos se **barren, no se registran como alias**. Default en español. | 2026-07-18 | reemplazada por 0057 | — |
| Local-0006 | Nomenclatura en español para los subsistemas | **Nomenclatura en español para todos los subsistemas del harness:** renombrar `memory/`→`memoria/` y su índice `MEMORY.md`→`MEMORIA.md`, para que ningún subsistema quede en inglés. La auto-memoria del sistema Claude Code (fuera del repo, `~/.claude/projects/.../memory/`) queda como está: nombre fijo del harness oficial, fuera de nuestro control. | 2026-07-18 | vigente | — |
| Local-0007 | De scripts a Herramientas | **El subsistema `scripts` se generaliza a `herramientas` (Tools):** catálogo de las *tools* que el Propósito del repo requiere. Tipos: `script`, `skill` local del repo, `MCP` local; hooks **afuera** (automatización disparada por evento, no invocable por el agente). Registro con columna **Tipo**; cada fila apunta a donde vive la tool (script en su carpeta bajo el subsistema, skill en `.claude/skills/`, MCP en `.mcp.json`). Nombre en español (0006). Motivación: "script" quedó corto para la categoría. | 2026-07-19 | vigente | — |
| Local-0008 | Lints co-ubicados con su subsistema | **Los lints se co-ubican con su subsistema, no centralizados:** cada subsistema es índice + entradas + **su lint** en su propia carpeta (`.claude/<sub>/lint-<sub>/`), alineando el disco con 0003 (el lint siempre fue parte del Patrón del subsistema). Disuelve `.claude/scripts/` como carpeta compartida y elimina el acoplamiento cruzado (cada funcionalidad instalaba su lint en territorio de `scripts`). `lint-harness` (transversal, no es de un subsistema) es Herramienta de dominio de **este** repo (autorar el harness) y vive en `.claude/herramientas/lint-harness/`. | 2026-07-19 | vigente | — |
| Local-0009 | Taxonomía de skills por ámbito | **Taxonomía de skills del harness:** las skills se clasifican por **ámbito**: *Skill de Subsistema* (opera sobre un subsistema concreto, viaja en el plugin de su funcionalidad — incluye las de prácticas como cerrar un plan) y *Skill del Agente Multipropósito* (transversal, sin subsistema dueño, funcionalidad propia — hoy `planificar`). Las de autoría del harness no son una clase aparte: son Herramientas del Agente Desplegado que es este repo (0007), locales y no distribuibles. Criterio skill vs script: lo mecánico determinista va en script/lint; lo que requiere juicio o significado va en skill (que puede llevar scripts auxiliares). | 2026-07-19 | vigente | — |
| Local-0010 | Arquitectura multiagente con AGENTS.md | **Arquitectura multiagente del harness:** `AGENTS.md` en la raíz como fuente única de instrucciones (estándar de la Linux Foundation, leído nativo por Codex/Cursor/Gemini/Copilot) con `CLAUDE.md` reducido a adaptador de una línea (`@AGENTS.md`); los subsistemas quedan en `.claude/` como casa de datos única para todos los agentes; `SKILL.md` (estándar abierto Agent Skills) pasa a formato único y se retiran los `prompt.md`; skills distribuidas a nivel usuario con junctions dobles (`~/.claude/skills/` + `~/.agents/skills/`); hooks con registro doble (settings.json de Claude + hooks de Codex, mismo script). Paridad de comportamiento objetivo: Claude Code ↔ Codex CLI. | 2026-07-19 | vigente | [0010-arquitectura-multiagente.md](0010-arquitectura-multiagente.md) |
| Local-0011 | Responsabilidad del índice en el Patrón recursivo | **Responsabilidad del índice en el Patrón recursivo (0002):** cada índice responde por las páginas de su directorio y de los subdirectorios **sin índice propio**; un subdirectorio con su propio `INDICE.md` (una **Carpeta**, en términos del glosario) es **una sola Entrada** para el índice de arriba — se lo menciona una vez, no sus páginas internas. Un sub-índice se atribuye a su ancestro **estricto** más cercano, así el padre queda obligado a nombrarlo. Regla para el lint de cualquier subsistema: cada página se atribuye a su índice ancestro más cercano; el fallback por carpeta recorre la cadena de ancestros hasta el directorio del índice, no solo el padre inmediato; un hallazgo cae una sola vez y contra el índice que corresponde. (Motivada por un hueco de `lint-conocimiento` que hacía responsable al índice raíz por páginas ya delegadas a un sub-índice.) | 2026-07-20 | vigente | — |
| Local-0012 | La Pantalla de bienvenida se emite por systemMessage | **La Pantalla de bienvenida se emite vía `systemMessage`, no `additionalContext`:** un SessionStart hook solo escribe en la terminal del **usuario** con el campo `systemMessage`; `additionalContext` (lo que se usaba) llega **solo al modelo**, invisible al usuario — por eso no se veía nada. Es cross-agente: Claude Code (CLI), Codex y Gemini soportan `systemMessage` al arrancar; **Cursor no** tiene banner nativo (degrada sin caja, sin envoltorio de launcher). La caja es de **ancho automático** (se dimensiona al renglón más largo) para no desarmarse cuando una métrica gana dígitos. (Corrige una premisa falsa asentada el 26-07-20: que `additionalContext` era "full-visible en el transcript".) | 2026-07-21 | vigente | — |
| Local-0013 | Segmentación de skills por prefijo de plugin | **Segmentación de skills por prefijo de plugin:** las skills del harness se diferencian por subsistema con el prefijo nativo `plugin:skill` de Claude Code (`memoria:registrar-memoria`, `glosario:converger-terminologia`) — el prefijo **es** el nombre del plugin. **No** por renombre de skills ni por un prefijo único `amp:` (un único prefijo exige un mega-plugin que esconde el subsistema y rompe la instalación modular 0009). Exige que el prefijo sea el subsistema pelado: se limpian los nombres de plugin (`memoria-local`→`memoria`, `gestion-de-planes`→`planes`, `preferencias-trabajo`→`preferencias`, `estilo-commits`→`commits`; glosario/decisiones/conocimiento/herramientas ya están). La **máquina de autoría pasa de junction a instalación por plugin** (marketplace local) para que la segmentación sea visible, aceptando perder la edición en vivo (el plugin copia a cache). Codex/Cursor/Gemini quedan **diferidos**: instalan por junction (Agent Skills), sin mecanismo de prefijo ⇒ la segmentación es hoy Claude-Code-only y rompe la paridad 0010 a propósito. *(La cláusula del **prefijo pelado** fue **modificada por 0029**: el prefijo pasa a `amp-<sub>:`; el multi-plugin por subsistema y la segmentación por prefijo siguen vigentes.)* | 2026-07-21 | vigente | — |
| Local-0014 | Nombre del proyecto: Agentes Multipropósito | **Nombre del proyecto: "Agentes Multipropósito" (AMP) / "Multipurpose Agents" (MPA)** — en **una sola palabra**, la misma forma que el adjetivo (`el setup estándar multipropósito`). En singular, *Agente Multipropósito* es la línea de marca constante en todo repo consumidor. **Se evaluó y se descartó** la variante en dos palabras (*Agentes Multi Propósito*), que había quedado anotada como decidida el 20/07/2026: obligaba a sostener para siempre dos formas — el nombre separado y el adjetivo junto, porque separar el adjetivo rompe el castellano — y esa distinción era justamente lo que hacía reaparecer la discusión sesión tras sesión. Con una sola palabra no queda nada que distinguir: el glosario lleva **una fila única** con `AMP`/`MPA` como alias. **Se asienta acá porque hasta hoy el nombre vivía solo en notas de planes, que nadie consulta al arrancar** — por eso volvía. Pendiente aparte, por ser ruptura para los consumidores ya instalados: el nombre del marketplace (`xelnagah-harness`). | 2026-07-21 | vigente | — |
| Local-0015 | Nombres de skills y Herramientas en verbo más objeto | **Nomenclatura de skills y Herramientas en verbo+objeto:** el nombre es una acción (verbo + objeto: `registrar-memoria`, `inventariar-componentes-sueltos`, `instalar-junctions`), no un adjetivo ni una frase nominal (`fuera-de-subsistema` ✗). Excepción: los lints conservan `lint-<subsistema>` (nombre fijo del Patrón, decisión 0008). (Motivada al nombrar `inventariar-componentes-sueltos`, corrigiendo la propuesta adjetiva `fuera-de-subsistema`.) | 2026-07-21 | vigente | — |
| Local-0016 | Los nombres de skills y Herramientas se ratifican | **Los nombres de skills y Herramientas requieren ratificación del usuario**, igual que los términos canónicos (decisión 0004): el agente propone, no bautiza; ningún nombre se fija sin el ok explícito. Un nombre de tool es una interfaz estable que viaja a los repos consumidores. | 2026-07-21 | vigente | — |
| Local-0017 | Modelo de carga de contexto por subsistema | **Modelo de carga de contexto: descripción por subsistema siempre cargada; índice bajo declaración.** Lo que va siempre en contexto es una **descripción liviana por subsistema** (qué es, cómo se usa, cuándo consultarlo), no su índice. Cada descripción **declara si su índice también se carga** incluyendo —o no— la línea `@INDICE.md`: import **anidado** de Claude Code (cadena `CLAUDE.md→AGENTS.md→descripción→índice`, hop ≤3, bajo el límite documentado de 4). Como **no existe import condicional**, la presencia de la línea **es** la declaración y no puede divergir de la realidad (sin lint que la vigile). Se descartaron **M2** (AGENTS.md arma el import a mano honrando un flag → el flag puede mentir, exige lint) y **M3** (hook ensambla en runtime vía `additionalContext` → máquina extra, canal solo-modelo). **Revisa la cláusula "el `INDICE.md` va siempre en contexto" de 0002** (el resto de 0002 sigue vigente). Fuera de Claude Code (Codex/Cursor/Gemini no expanden imports) degrada a instrucción de lectura, igual que 0010. Motiva: `PLANES.md` era el índice más pesado (~21k chars, casi la mitad del contexto siempre cargado; la mitad de sus filas eran terminales históricas). | 2026-07-21 | vigente | — |
| Local-0018 | Términos por estado en el glosario, con veto | **Modelo de términos por estado en el glosario + veto (extiende 0004):** cada concepto agrupa sus términos en un solo eje de estado — `canónico` (la celda Concepto), `alias` (válido, ratificado), `propuesto` (sugerido por el agente, sin usar hasta ratificar) y `vetado` (prohibido; el reemplazo es el canónico de su propia fila). La tabla pasa a 6 columnas (`Concepto \| Definición \| Alias \| Propuestos \| Vetados \| Detalle`). **Gobernanza:** el agente nunca escribe en `Alias` ni en `Vetados` —solo propone en `Propuestos`—; ratificar y vetar son del usuario; el agente nunca usa un término propuesto o vetado. Extiende 0004 (que cubría conceptos) a los **alias**, y agrega **prohibir**, no solo registrar. Los vetados sin concepto de dominio propio (anglicismos, copias literales del inglés, jerga) se agrupan bajo el concepto **Terminología Farlopa**, con su mapa término→reemplazo en la página de Detalle (fenómeno universal, en el conocimiento `terminologia-farlopa`). El `lint-glosario` gana: contradicción alias/vetado, vetado ambiguo (mismo término en dos filas → pedir Detalle), propuestos pendientes (recordatorio) y apariciones de vetados en el repo (dos grupos: texto plano accionable / código informativo; autoexcluye glosario y planes ejecutados/descartados). Motivada: registrar un alias lo legitima ⇒ un término confuso queda como válido y el agente lo reusa. | 2026-07-21 | vigente | — |
| Local-0019 | Cableado y estructura del Manifiesto de subsistema | **Cableado y estructura del Manifiesto de subsistema (extiende 0017):** los manifiestos se importan **directos** en una sección `## Subsistemas` de `AGENTS.md` (0010), **no** vía un archivo agregador — un agregador suma un hop (3→4, al ras del límite documentado de 4, margen 0) y duplica la indirección para los agentes que no expanden imports (Codex/Cursor/Gemini); solo se justificaría si llevara **contenido propio** (autodescripción del Agente Multipropósito completo), no como mero cargador. **Estructura obligatoria** de cada `MANIFIESTO.md`: 5 campos — (1) título, (2) identidad (qué es + dónde vive), (3) **disparador** (cuándo se consulta y cuándo se escribe), (4) declaración explícita de carga del índice (`se carga siempre` / `NO se carga siempre` + cuándo consultarlo a demanda), (5) lint al cerrar — más la línea `@INDICE.md` presente **sólo si** el índice se carga (su presencia ES la declaración, M1 de 0017). **Sin frontmatter** (un flag de carga resucitaría el M2 descartado por 0017). El chequeo de que el manifiesto tenga los campos vive en `lint-harness` (lado autor, junto al chequeo de tamaño de 0017), **no viaja** al consumidor (se instala correcto desde PLANTILLA). | 2026-07-22 | vigente | — |
| Local-0020 | Test de demarcación | **Test de demarcación: dónde va cada archivo o carpeta se decide por 5 ejes** — de quién es (Producto del Propósito ↔ harness) · qué relación tiene el agente con la cosa (la lee ↔ la sabe ↔ la ejecuta) · cambia o está fijo (estado vivo ↔ veredicto) · es invocable por sí mismo (fila del registro ↔ contenido interno) · sustantivo o verbo (glosario ↔ Herramienta/comando). Los ejes son el test; los casos resueltos y los criterios finos viven en el detalle. Materializa el frente A del plan *efecto conductual*: baja a un registro consultable las reglas de demarcación que se acordaban y se evaporaban. | 2026-07-22 | vigente | [0020-test-de-demarcacion.md](0020-test-de-demarcacion.md) |
| Local-0021 | Subsistema conducta | **Subsistema `conducta`: reglas "cuando hagas X, asegurate de Y" atadas a momentos del flujo.** Ata **momentos** (evento de hook + condición sin juicio) a **acciones** (inyectar texto / correr una Herramienta / bloquear), entregadas por un hook repartidor por evento que lee un registro compartido. Agente-agnóstico: cada agente declara con qué mecanismo realiza cada momento (Claude por `PreToolUse`; Codex desviando por Bash; o "no realizable", documentado). Viene con una **Base** instalada (respetar preferencias, considerar conocimiento, registrar cuando algo cambia) + reglas del Agente Desplegado que es cada repo. Se apoya en los otros subsistemas, no los duplica. Descarta la config fija como solución única (agregar reglas a mano sin lint es la conducta que el harness combate). Absorbe los planes de control en el punto de acción. | 2026-07-22 | vigente | [0021-subsistema-conducta.md](0021-subsistema-conducta.md) |
| Local-0022 | Autodescripción por skill transversal | **Autodescripción del mecanismo de los subsistemas = una skill transversal que ensambla, no docs nuevas.** El pedido "que el agente explique cómo funciona cada subsistema y el Agente Multipropósito completo" se resuelve con **una** Skill del Agente Multipropósito (0009) —`explicar-subsistema <sub>`, verbo+objeto (0015), nombre provisional a ratificar (0016)—, empaquetada como funcionalidad propia (como `planificar`; operacional ⇒ sin lint, 0003). **Ensambla** la respuesta desde las fuentes ya instaladas bajo `.claude/<sub>/` —`MANIFIESTO.md` (qué es + disparador), las skills del subsistema (skills + flujo, que el modelo ya tiene en su catálogo de skills), `amp-info` (estado), y `feedback_<sub>` para el orden multi-paso— narrando a profundidad variable por juicio (default media; profundiza a demanda). Sin argumento explica la articulación del Agente Multipropósito completo (fuente `AGENTS.md` + decisiones). **Descarta** una skill por subsistema (8 copias del mismo flujo a sincronizar) y la sección en `INDICE` (engorda el contexto siempre-cargado, contra 0017; nada obliga a contestar así). No se escribe doc consolidada nueva: la dispersión en 4 lugares (SKILL, feedback, README de plugin, AGENTS.md) se resuelve **designando la fuente canónica por faceta**, no agregando una quinta. La mitad "en qué estado está" ya la cubre `amp-info`; el bloqueo de nomenclatura no aplica (los nombres de subsistema son firmes). | 2026-07-23 | reemplazada por 0023 | — |
| Local-0023 | Autodescripción por Manifiesto enriquecido | **Autodescripción vía MANIFIESTO enriquecido (reemplaza 0022, extiende 0019).** El pedido "que el agente explique cómo funciona cada subsistema" **no** se resuelve con una skill nueva que ensambla en runtime (0022, descartada por sobreingeniería), sino enriqueciendo el propio `MANIFIESTO.md` —siempre cargado— para que **sea** la fuente. Se le suman dos campos: **Skills** (obligatorio — nombra las skills de **operación** del subsistema y qué hace cada una; la de **instalación** `inicializar-<sub>` se menciona al pasar; `conducta` pone "ninguna aún") y **Flujo de trabajo** (opcional — solo si es multi-paso, y como **puntero** al detalle on-demand en `feedback`/`SKILL`, nunca el paso a paso inline, para no engordar el contexto de arranque, 0017). Preguntar "¿cómo funciona X?" se contesta desde el MANIFIESTO ya en contexto + `amp-info` (estado), sin Componente de Subsistema nuevo. **Anatomía del MANIFIESTO:** obligatorios — Título · Identidad · Disparador · **Skills** · Carga del índice · Lint; opcionales — Flujo · Gobernanza · Advertencias · Prueba de pertenencia; **una sola clase**, núcleo obligatorio + bloques opcionales que se prenden según el subsistema. El chequeo de campos vive en `lint-harness` (lado autor, no viaja). El **ordenamiento de los skills** en sí (ejecutar el prefijo 0013, renombrar los opacos, cerrar huecos de cobertura) ya vive en 3 planes abiertos (nomenclatura, prefijo/distribución, cobertura por subsistema), no necesita uno nuevo. | 2026-07-23 | vigente | — |
| Local-0024 | El texto distribuido no cita decisiones del harness | **El texto distribuido a un consumidor no expone el registro de decisiones del harness.** Los Componentes de Subsistema que un `inicializar-*` escribe en el repo destino —lints `.js`, `MANIFIESTO.md` (los 6), docs de gobernanza (`glosario/INDICE.md`, `feedback_glosario.md`), bloques que se agregan a `AGENTS.md`, las PLANTILLAs y sus gemelos Textual en el orquestador— enuncian la regla o la razón **inline, sin citar el número** de decisión del harness. **Regla operativa:** un número de decisión solo aparece en archivos que **nunca** se instalan en un consumidor (`SKILL.md`/`README.md` de instalación, páginas de detalle, planes, texto plano propio de `AGENTS.md`). Motivo: el número referencia el registro de **este** repo, que no viaja; en el consumidor colgaba (10+ refs sobre 6 decisiones citadas: 0004/0008/0010/0011/0017/0019, esta última en cada MANIFIESTO). **Camino 2 (sacar el número)** sobre Camino 1 (instalar las decisiones-base en el consumidor): evita ensuciar el registro del consumidor con arquitectura interna del harness (choca con 0001) y **disuelve** la ambigüedad de numeración (el registro del consumidor arranca en 0001) en vez de administrarla con rango reservado. `lint-harness` gana una guarda que falla si un archivo distribuible cita `decisión NNNN`. Cruza con `Separar mecánica del harness de criterio del autor`. | 2026-07-23 | vigente | — |
| Local-0025 | Control de Terminología Farlopa en dos niveles | **Control de Terminología Farlopa en dos niveles según persistencia, dentro de `conducta` (extiende 0021/0018).** La fricción se pone donde el daño se acumula. **Conversación** (`cada turno`, `inyectar`): recordatorio soft del criterio de demarcación; el agente decide, sin frenar ni ruido extra — minimiza la aparición y **acepta fugas** (la conversación es efímera, bajo daño). **Persistencia** (`al escribir`), dos velocidades porque el daño real es que la Terminología Farlopa se replica y reaparece desde los archivos: (a) **vetado conocido → `bloquear`** — `PreToolUse` corre `lint-glosario` sobre el contenido y deniega la escritura (programa, no se recita, falso positivo imposible); (b) **Terminología Farlopa nueva → `inyectar` fuerte** — recordatorio que fuerza aplicar el criterio antes de persistir (juicio; no se puede frenar limpio porque no está en ninguna lista). **A demanda:** `converger-terminologia` (skill) barre lo persistido y engorda vetados; queda **fuera** del disparo automático (un hook no corre skills, y un skill que el agente elige invocar recae en el modo 1 "recita sin obedece"). **El criterio es relativo al usuario del Propósito del repo, no al autor** (un dev entiende `hook`/`deploy`; el usuario de un repo contable no) ⇒ el control viaja al consumidor y se recalibra por Propósito, separando criterio de autor de mecánica. Fundamento: conocimiento `modos-de-falla-ante-reglas-escritas` (lo mecánico como programa; lo de juicio re-inyectado cerca del momento). Materializa el frente C de *Que el harness tenga efecto conductual* para la clase terminología; los 3 planes de punto-de-acción consumen los mismos momentos con distinto contenido, sin fusionarse. | 2026-07-23 | vigente | — |
| Local-0026 | Subsistema semántica, con dos registros pares | **Subsistema `semántica`, que rebautiza `glosario`, con dos registros pares.** El nombre `glosario` quedó corto para lo que el subsistema hace —mantener la coherencia semántica del dominio en el tiempo, no solo definir palabras—, así que se generaliza y se renombra, igual que `scripts` pasó a `herramientas` en la Decisión Local-0007 (de scripts a Herramientas). Sostiene **dos registros pares** bajo un `lint-semantica` y un `MANIFIESTO`, ninguno cargado en contexto siempre: `GLOSARIO.md` (terminología legítima) y `TERMINOLOGIA-FARLOPA.md` (relaciones vetadas). Terminología Farlopa **deja de ser una fila del glosario con un mapa colgado** —la disposición que había fijado la Decisión Local-0018 (términos por estado en el glosario, con veto)— y pasa a registro propio; el glosario pierde su columna `Vetados`, y todo lo vetado, con reemplazo o sin él, vive en el registro par. Los vigila el control en dos niveles de la Decisión Local-0025 (control de Terminología Farlopa en dos niveles). Cómo opera cada registro lo documenta el propio subsistema. | 2026-07-23 | vigente | [0026-subsistema-semantica.md](0026-subsistema-semantica.md) |
| Local-0027 | Separación por origen en los subsistemas | **Separación por origen (Base vs aprendido) en los subsistemas, por granularidad — precondición del nivelador `amp-actualizar`.** El harness distingue lo que manda (**Base**, río arriba) de lo que el Agente Multipropósito aprende persiguiendo su Propósito (**aprendido**), para que el nivelador reemplace lo Base sin abrir lo del repo. Se realiza distinto según la granularidad del subsistema: **por fila** (conducta) → dos secciones con tabla propia (`## Reglas del Agente Multipropósito` / `## Reglas del Agente Desplegado`), molde de `PREFERENCIAS.md`, que separa igual; **por archivo** (memoria) → los archivos Base tienen nombre propio y el nivelador los pisa por nombre, los aprendidos quedan intactos (su rediseño de fondo va aparte); **contenido puro aprendido** (glosario, semántica, conocimiento, decisiones, planes, herramientas) → el harness los instala vacíos y el nivelador de rutina **no los reescribe**; el mecanismo (lint, MANIFIESTO, estructura) es Base y se pisa entero. **No precluye** despachar contenido sembrado en glosario/semántica/conocimiento a futuro: sería una migración dedicada, no tarea del nivelador de rutina. Extiende el Patrón de subsistema (0002) con el origen como dimensión. | 2026-07-24 | vigente | — |
| Local-0028 | Diseño del nivelador | **Diseño del nivelador `amp-actualizar`: convergencia estructural sin estado, con respaldo y reporte (extiende 0001, se apoya en 0027).** Un solo skill pone al día el `.claude/` de un repo instalado contra la plantilla objetivo, **sin guardar número de versión**: converge por estructura (agrega lo ausente; aplica renombres conocidos —`glosario`→`semantica`— al detectar la forma vieja) en vez de correr una lista de migraciones versionadas (los repos viejos no tienen versión guardada; la vía versionada necesitaría igual un arranque estructural). **Base** (mecanismo del harness: lint, MANIFIESTO, estructura, cableado del hook) → **pisar sin preguntar, respaldando** la versión vieja antes (`.claude/.respaldo-amp/<fecha>/`) y reportando al final solo lo que cambió; el respaldo importa porque `.claude/` suele estar gitignoreado en el host (sin red de git). **Aprendido** (contenido del repo) → nunca se toca. **Reacomodo legacy** (primer contacto, archivos mezclados pre-0027) que pueda enredar contenido aprendido → **preguntar** antes (bloqueante). El respaldo es salida, no estado leído de vuelta ⇒ no rompe el sin-estado. Nombre a ratificar (0016). | 2026-07-24 | vigente | — |
| Local-0029 | Empaquetado en un plugin por subsistema | **Empaquetado del harness: un plugin transversal `amp` + un plugin `amp-<sub>` por cada subsistema con skill de operación (modifica 0013, refina 0009).** El harness se distribuye como marketplace con prefijo `amp-`: **`amp` transversal** (prefijo `amp:`, skills `inicializar` · `planificar` · `info` · `actualizar`) y un plugin por cada subsistema que tenga skill de operación —`amp-memoria`, `amp-conocimiento`, `amp-decisiones`, `amp-preferencias`, `amp-planes`, `amp-semantica`— con prefijo `amp-<sub>:` que **agrupa** al tipear "amp" **y** deja **visible** el subsistema (`amp-planes:ciclo-de-plan` ≠ `amp-conocimiento:registrar-conocimiento`; saber de qué subsistema es cada skill es el objetivo). Subsistema **sin** skill de operación (`herramientas`, `conducta`, `commits`) **no tiene plugin** hasta que gane una; su estructura la escribe `amp:inicializar`. **Bundle completo, no à la carte:** todo Agente Multipropósito instala los 6 `amp-<sub>` vía `dependencies` del plugin `amp` ⇒ **1 install/repo**, **project scope** (por repo, no user — el harness solo aplica a repos con Agente Multipropósito; un repo sin harness no lleva estas skills). *(La cláusula del **alcance** fue **modificada por 0035**: pasa a `local`; la instalación única por repo y el descarte de `user` siguen vigentes.)* **Consolida los 10 `inicializar-<sub>` en un solo `amp:inicializar`** que escribe la estructura completa (refina 0009 y absorbe el orquestador `setup-completo`; sin argumento à la carte porque el bundle es siempre todo). **Modifica 0013:** el prefijo pasa de subsistema **pelado** (`memoria:`) a `amp-<sub>:`; el multi-plugin por subsistema y la segmentación por prefijo de 0013 **quedan**. Skills transversales renombradas (ratificadas): `inicializar-custom`→`amp:inicializar`, `amp-actualizar`→`amp:actualizar`, `amp-info`→`amp:info`, `planificar`→`amp:planificar`. **12 plugins → 7.** Absorbe los puntos 2/3/4 del plan `Modelo de distribución y empaquetado del harness`. Codex/Cursor/Gemini siguen diferidos por junction (el prefijo es mecanismo de Claude Code). Ejecución (migrar marketplace + carpetas + `dependencies` + renombres) pendiente como plan. | 2026-07-24 | vigente | — |
| Local-0030 | La Pantalla de bienvenida es una regla de conducta | **La Pantalla de bienvenida es una Regla Base del subsistema `conducta` (clase `correr`, momento `al arrancar la sesión`), no una Herramienta.** El bloque de estado que se emite al arrancar la sesión pasa a ser un comportamiento de Base de `conducta` ("al arrancar la sesión, mostrá la pantalla"): momento nuevo `al arrancar la sesión` (evento `SessionStart`, sin condición) en `MOMENTOS.md`; regla Base clase `correr` en `conducta/INDICE.md` cuyo Contenido es la ruta del script; el script se **co-ubica** con conducta (`.claude/conducta/mostrar-pantalla-bienvenida/`) y **sale** del registro de Herramientas; el repartidor `establecer-conducta` **implementa la clase `correr`** (ejecuta el script y reenvía su stdout carácter a carácter — `{systemMessage}` en SessionStart) y se cablea a `SessionStart`. `amp:info` muestra la misma pantalla a demanda. **Extiende 0021** (primera realización de la clase `correr` + primer momento `SessionStart`), se apoya en **0012** (emisión por `systemMessage`) y **0008** (infra Base co-ubicada, fuera del registro de Herramientas). Motivo: distribuir la pantalla a **todos** los Agentes Multipropósito por el mecanismo que ya viaja (conducta, dentro de `amp:inicializar`), en vez de un hook suelto + Herramienta que el orquestador no instalaba (hueco: un Agente Multipropósito consumidor no la recibía). | 2026-07-24 | vigente | — |
| Local-0031 | La documentación humana vive en `docs/` | **La documentación humana del proyecto vive en `docs/` en la raíz; la raíz queda solo con las otras tres clases de texto.** Fuera de `.claude/` se distinguen cuatro clases: **punto de entrada** (`AGENTS.md`, `CLAUDE.md` — obligado por cada agente), **portada** (`README.md` — convención de GitHub), **registro con lint** (`REGISTRO.md`, que `lint-harness` lee como fuente de datos: no es documentación) y **documentación humana** (guías y referencias que se leen una vez adentro: `INSTALAR.md` y las que vengan) → `docs/`. Mantiene la raíz como escaparate del Producto del Propósito, evitando que cada página nueva caiga al lado del Producto. **Convención de este repo solamente:** `amp:inicializar` **no** la instala en los consumidores — la documentación es del lado Producto, y del lado Producto el harness no manda (cada repo documenta su propio Propósito donde quiera). Se apoya en el Test de demarcación (0020, ejes 1 y 2) para descartar `.claude/`, pero **no se deriva** de él: elegir el nombre `docs/` —sobre `documentacion/` y sobre dejar los archivos sueltos— es convención, no aplicación de un eje; gana `docs/` porque GitHub le da trato especial y el repo es público. Motiva: `INSTALAR.md` estaba bloqueado por no saber dónde ponerlo, y el plan «Publicar el harness en inglés» ya implica un segundo archivo. | 2026-07-25 | vigente | — |
| Local-0032 | Las Herramientas se separan por origen | **Las Herramientas se separan por origen: `herramientas/INDICE.md` gana dos secciones — `## Herramientas del Agente Multipropósito` y `## Herramientas del Agente Desplegado`.** Aplica a `herramientas` la realización **por fila** de 0027 (dos secciones con tabla propia) que ya usan `conducta` y `PREFERENCIAS.md`: el nivelador `amp:actualizar` reemplaza la sección Base entera y **nunca abre** la del Propósito; una Herramienta que fabrica el repo va siempre a la segunda. **Modifica 0027** (que sigue vigente): su cláusula clasificaba `herramientas` como *contenido puro aprendido* —el harness lo instala vacío—, pero eso describía el estado de entonces, cuando ninguna Herramienta era Base; no era una prohibición. Motiva: `actualizar-plugins` —pone al día los plugins del Agente Multipropósito y detecta el desfase entre lo que corre y lo publicado— es **infra del harness que tiene que viajar a los consumidores**, y no es una tool del Propósito de un repo de contabilidad o de análisis de mudanza: necesitaba lado Base. **Descartadas:** (a) una categoría estructural nueva para infra sin subsistema dueño (`.claude/amp/`), que suma un componente que el Patrón no contempla y que `inventariar-componentes-sueltos` marcaría; (b) co-ubicarla con un subsistema como hizo 0030 con la Pantalla de bienvenida — no tiene subsistema dueño. Consecuencia: `herramientas` deja de ser "puro aprendido" y pasa a mixto por sección, como `conducta`. | 2026-07-25 | vigente | — |
| Local-0033 | Poner al día un Agente instalado son dos fases | **Poner al día un Agente Multipropósito instalado es un proceso de dos fases, cada una con su ejecutor.** La **fase de plugins** (qué plugins hay en la máquina, en qué versión y alcance) la ejecuta la Herramienta Base `actualizar-plugins`; la **fase de archivos** (el contenido de `.claude/`: lints, manifiestos, estructura) la ejecuta la skill `amp:actualizar`. **Orden de ejecución: plugins → reinicio → archivos** — los archivos los escribe una skill que viaja **dentro** del plugin, así que nivelarlos antes los pone al día con una versión vieja del propio instalador. **Punto de entrada único:** `amp:actualizar` arranca diagnosticando la fase de plugins y, si hay desfase, lo resuelve y **pide reiniciar en vez de seguir**; se la vuelve a pedir después del reinicio y ahí sí nivela archivos. Así se recuerda un solo nombre y el orden lo garantiza la skill, no la memoria del usuario. **Arranque:** un repo instalado antes de que la Herramienta existiera no la tiene y no puede usarla para actualizarse; esa primera pasada corre los comandos del CLI una sola vez (documentados en el manual de instalación), y de ahí en adelante la Herramienta ya está instalada. Por eso el manual sigue existiendo: cubre la instalación de cero y ese arranque, no la operación de rutina. **Extiende 0028** (que definió el nivelador como convergencia de archivos): no le cambia el alcance —sigue sin tocar plugins— sino que le suma un control al entrar. | 2026-07-25 | vigente | — |
| Local-0034 | El Agente con Propósito contiene al Agente Multipropósito | **El Agente con Propósito contiene al Agente Multipropósito: `Agente Multipropósito + Propósito = Agente con Propósito`.** El término *Agente Multipropósito* nombraba dos cosas a la vez —lo que este repo publica y lo que queda instalado en cada repo—, que se ponen al día por vías distintas y de las cuales solo una tiene versión. Se parte en dos conceptos y se nombra el tercero que faltaba: **Agente Multipropósito**, el setup sin Propósito definido, que es lo que este repo publica y **lo único versionado**, y que tiene **dos partes que viajan por caminos distintos** —sus archivos y sus skills—; **Agente con Propósito**, el que nace cuando alguien le define un Propósito y evoluciona persiguiéndolo; y **Aprendizaje**, las entradas que ese Agente acumula en sus subsistemas. La relación es de **composición, no de estados sucesivos**: el Agente Multipropósito sigue estando adentro como su parte Base, y es esa parte —no el todo— la que el nivelador pone al día. De las dos partes se sigue que poner al día sean dos fases, que es lo que fija la Decisión Local-0033 (poner al día un Agente instalado son dos fases). **Precisa la Decisión Local-0014 (nombre del proyecto: Agentes Multipropósito) sin reemplazarla.** Las definiciones completas viven en el glosario. | 2026-07-25 | vigente | [0034-agente-con-proposito.md](0034-agente-con-proposito.md) |
| Local-0035 | El Agente Multipropósito se instala con alcance local | **El Agente Multipropósito se instala con alcance `local`, no `project` (modifica la cláusula de alcance de 0029).** La 0029 fijó `project` comparándolo contra `user` — `local` nunca se evaluó. Medido el 26/07/2026: se armó un repo cuyo `.claude/settings.json` declaraba `enabledPlugins` con `amp@xelnagah-harness` **sin instalarlo**, se abrió una sesión ahí y **no tenía la skill**, ni apareció entrada en `installed_plugins.json`. O sea que la declaración commiteada **no hace que el plugin llegue**: el alcance `project` promete *"install for all collaborators"* y no lo cumple —quien clone el repo tiene que instalar igual a mano— mientras deja configuración de máquina dentro de un archivo versionado. Con `local`, esa declaración va a `.claude/settings.local.json`, que no se commitea, y el repo deja de arrastrar estado de una instalación que solo vale en esta máquina. **Lo demás de 0029 no se toca**: sigue siendo **1 instalación por repo** (el plugin transversal `amp` trae los seis `amp-<sub>` por dependencias) y sigue **sin ser `user`**, por el mismo motivo de siempre: el harness aplica a los repos que lo usan, no a todos los que se abran. ⚠️ **Los dos alcances conviven durante la migración**, y confundirlos falla en silencio: un `update` o un `uninstall` con el alcance equivocado no encuentra el plugin y no toca nada. | 2026-07-26 | vigente | — |
| Local-0036 | Memoria se retira y nace subsistemas | **Memoria se retira y se transforma en `subsistemas`; el detalle de cada subsistema pasa a un `README.md` propio (modifica 0006 y 0029, cumple mejor 0023).** Memoria fue el primer subsistema, así que absorbió todo lo que no tenía dónde ir; lo que hoy cuelga de ella no es diseño, es residuo. Medido sobre tres repos: **8 memorias Base idénticas** que describen subsistemas, más memorias del Propósito que son **conocimiento mal ubicado** (en Impresión3d, la red de una impresora K1C sigue siendo cierta sin el repo). En el repo autor las 10 son `type: feedback`, un tipo que quedó sin razón de ser cuando nació `preferencias`. **Se decide:** (a) memoria no sobrevive — el subsistema pasa a ser **`subsistemas`**, el catálogo de subsistemas del Agente Multipropósito, con `SUBSISTEMAS.md` en el contexto siempre cargado, dos secciones por origen, lint de catálogo↔disco y la habilidad `agregar-subsistema`, que pone el Patrón a disposición del Propósito de cada repo; (b) cada subsistema gana un **`README.md`** a demanda con su explicación entera, y el `MANIFIESTO.md` deja de mandar a leer — `feedback_flujo_planes` no era una memoria mal puesta por su contenido, era el README de planes en la carpeta equivocada, y el repo ya usa esa convención 15 veces con los README de lints y scripts; (c) los hechos contingentes del proyecto van a **conocimiento** sin secciones nuevas, y se **borra su prueba de pertenencia**, que existía solo para separarse de memoria; (d) `herramientas` y `conducta` reciben `registrar-herramienta` y `registrar-regla`; (e) una sola habilidad de una corrida reparte lo mal ubicado, preguntando de a un Componente de Subsistema por vez — **no** la hace el nivelador, cuyo principio fundante (0027/0028) es no abrir nunca el Aprendizaje. **Se descartó** un orquestador con registro de propuestas: necesita archivo de estado entre sesiones, y estado + entradas + lint **es** el Patrón de 0002 — la tabla deriva sola en un subsistema más, que es el modo de falla que esto deshace. **Consecuencias:** 7→9 plugins (`amp-memoria` queda **retirado**, entran `amp-herramientas`, `amp-conducta` y `amp-subsistemas`) y se disuelven las cuatro dependencias sobre `amp-memoria`, que existían solo para sembrar las `feedback_<sub>`. El contexto de arranque no crece: los manifiestos siguen pesando ~9 KB y el detalle (26 KB) queda a un salto, no a una cadena. | 2026-07-26 | vigente | — |
| Local-0037 | La validación de terminología alcanza a lo que se publica | **La validación de terminología alcanza al Agente Multipropósito que este repo publica, y falla cuando encuentra términos vetados.** Hasta ahora la validación miraba solo el Agente Multipropósito **instalado en este repo** (hoy `.claude/`). El que **se publica** —el Producto del Propósito de este repo, hoy `funcionalidades/`— quedaba afuera, y es el que se copia a cada **agente desplegado**: un término vetado ahí se replica a agentes que ya no se arreglan editando un archivo, hay que nivelarlos uno por uno. Por eso en lo que se publica la validación **falla** y el texto se corrige antes de seguir; en lo instalado acá, que no sale del repo, el término se lista sin fallar y se corrige al editar ese archivo. La validación se sumó al lint que **se queda en el repo que publica** (hoy `lint-harness`), no al que **se instala en cada agente desplegado** (hoy `lint-semantica`), que allá no tendría nada que validar. Medido el 26/07/2026: lo que se publica nunca se había barrido y aun así ningún chequeo lo marcaba — los términos aparecían listados en la salida, pero ninguno los sumaba, así que el resumen daba todo bien. | 2026-07-26 | vigente | — |
| Local-0038 | El control al escribir frena por término sin uso legítimo | **El control de terminología en el momento `al escribir` frena por término sin uso legítimo posible, no por término vetado (modifica el punto (a) de 0025).** La 0025 previó *vetado conocido → `bloquear`*; medido, eso frena escrituras legítimas: el Frente A del barrido detectó **41 apariciones y solo 14 eran reales**, y un bloqueo por término dejaría inescribible al propio `TERMINOLOGIA-FARLOPA.md`, que contiene los vetados por definición. El control queda en **dos velocidades según la columna `Control` del registro**: `bloquea` (anglicismos puros como `levelear`, donde el falso positivo sí es imposible ⇒ se cumple el criterio de la clase) e `avisa` (los que dependen del significado ⇒ la máquina marca y el agente juzga, el reparto que fija 0026). **El bloqueo mira solo las apariciones fuera de comillas simples invertidas**: citar un término para hablar de él es legítimo y frecuente —la Base de preferencias lo hace al dar ejemplos—, así que se frena usarlo, no nombrarlo. `semantica/` queda exenta. Además el momento **amplía su condición** de `.md` bajo `.claude/` a `.md` de cualquier parte del repo salvo `tmp/`: lo que se publica es por donde entró la terminología ajena, y en un repo de contabilidad cubre el informe al cliente. El repartidor pasa a **combinar** clases en un mismo momento (el texto fijo de las `inyectar` con lo que miden las `bloquear`), y realiza la clase `bloquear` por primera vez. **Paridad Codex:** el momento sí es realizable allá (`apply_patch` dispara `PreToolUse` y matchea como `Edit`/`Write`), pero el `deny` todavía no frena la escritura (bug abierto del CLI) ⇒ ahí degrada a aviso; se emite igual para que frene solo cuando lo arreglen. | 2026-07-26 | vigente | — |
| Local-0039 | Distribución de habilidades para Claude Code y Codex CLI | **Distribución de habilidades para Claude Code y Codex CLI.** Se retiran los enlaces de directorio como forma de instalación. Ambos agentes obtienen las habilidades mediante los Plugins publicados por este repositorio. Claude Code instala el paquete completo en un paso. Codex CLI usa un instalador que agrega primero las dependencias y luego el Plugin principal, porque su comando `codex plugin add` no las instala automáticamente. | 2026-07-27 | vigente | — |
| Local-0040 | Las habilidades que clasifican Aprendizaje viven en su destino | **Las habilidades que clasifican Aprendizaje viven en el subsistema de destino; `amp-subsistemas:reubicar-aprendizaje` solo las coordina.** Cada subsistema conserva su criterio y puede invocarse por separado; la coordinadora compara destinos y propone la reubicación de a un Componente de Subsistema por vez, sin estado persistente. | 2026-07-27 | vigente | — |
| Local-0041 | El actualizador prepara plugins antes de nivelar archivos | **El actualizador prepara el marketplace y el paquete de plugins del agente que se está usando (Claude Code, Codex, etc.) antes de nivelar los archivos del repo.** La Herramienta distribuida lleva el marketplace, su origen Git y el paquete principal; detecta si corre en Claude Code o Codex y consulta la configuración correspondiente. En Codex, si falta el marketplace, `--aplicar` lo agrega, lo actualiza y reinstala el paquete completo por dependencias antes de exigir reiniciar la sesión. Motiva el fallo medido: Claude Code tenía el paquete registrado mientras Codex no tenía marketplace ni plugins, y el diagnóstico de Claude daba una respuesta tranquilizadora pero falsa. | 2026-07-27 | vigente | — |
| Local-0042 | Un subsistema tiene uno o más Índices, declarados | **Un subsistema tiene uno o más Índices de Subsistema, y cada uno declara en su frontmatter su nombre, su origen y sus columnas.** Hay dos cuando el contenido viene de dos orígenes y uno solo cuando viene de uno: **no se crean archivos vacíos**, y el segundo se suma el día que llegue. El campo `origen` decide el trato del nivelador —`agente-multiproposito` se reemplaza entero, `agente-desplegado` no se abre— y **el nivelador identifica el archivo por su frontmatter, no por su nombre**, así que el nombre deja de codificar el origen y queda descriptivo. Las **columnas declaradas** cierran un fallo silencioso: el lint compara el encabezado real de la tabla contra lo declarado y **falla en los dos sentidos**, la declarada que no está y la que está sin declarar. Cada `MANIFIESTO.md` gana un **sexto campo obligatorio** con la lista de sus Índices y el origen de cada uno, para que se descubran sin recorrer el directorio ni conocer una convención de nombres; lo lista como texto fijo del Agente Multipropósito, que el nivelador copia literal de la plantilla. **El frontmatter es la autoridad y el lint valida el manifiesto contra él.** Donde el Agente Multipropósito tiene contenido, `amp:inicializar` instala **también** el Índice del Agente Desplegado ya declarado —frontmatter completo y tabla sin filas—, para que las habilidades que escriben entradas siempre encuentren un Índice. Las columnas del Índice del Agente Multipropósito las declara él; el del Agente Desplegado declara **al menos** esas mismas y puede sumar las suyas. Modifica la Decisión Local-0002 (patrón de subsistema: índice, entradas y lint), extiende la Decisión Local-0019 (cableado y estructura del Manifiesto de subsistema) y realiza la Decisión Local-0027 (separación por origen en los subsistemas). | 2026-07-29 | vigente | [0042-indices-declarados.md](0042-indices-declarados.md) |
| Local-0043 | Núcleo del Índice de Subsistema | **Todo Índice de Subsistema comparte el núcleo de cuatro columnas —`Código`, `Nombre`, `Descripción` y `Detalle`—, y cada entrada lleva un código con prefijo de origen (`Base-0007` / `Local-0003`) que se asigna al crearla y no se reusa (modifica 0042).** Los encabezados del núcleo son literales en todos los Índices; lo que dejan de decir lo declara el frontmatter. Cada subsistema le suma solo las columnas operativas que su código consume. | 2026-07-29 | vigente | [0043-nucleo-del-indice-de-subsistema.md](0043-nucleo-del-indice-de-subsistema.md) |
| Local-0044 | El término montado sobre una palabra corriente se registra como expresión | **Cuando el término ajeno se monta sobre una palabra corriente del español, la Terminología Farlopa registra la expresión, no la palabra** (`capa de plugins`, no `capa`). Un registro de vetos enumera lo prohibido, que es finito; enumerar los usos válidos de una palabra corriente no cierra nunca, porque son infinitos. De ahí que **se descarte** una columna de usos legítimos, que convertiría la tabla en lista de lo permitido, y también la lista de excepciones por fragmento que `lint-harness` tenía en su código, ya retirada. **Una fila se califica por lo que acierta** —de las veces que marca la palabra, cuántas están de verdad mal usadas—, no por la forma de su Nombre: `capa` marcaba 37 apariciones y acertaba en ninguna, mientras la fila `prosa` marca 6 y acierta 6. Medido el 30/07/2026: reformular `capa` y `Adaptaciones` llevó el informe de 92 a 39 apariciones sin perder detección. El registro ya seguía este criterio en seis filas —`cementerio de tools`, `reforma de disco`, `Herramientas Base` y hermanas, que aclaran en su texto que `Base` sola no se veta—; estas dos eran las que habían quedado con la palabra pelada. **Complemento:** las comillas valen como cita en los tres lectores del registro, porque en español son la marca de cita y sin ellas no se puede escribir sobre la propia terminología; la cursiva no, porque marca cita pero también énfasis. | 2026-07-30 | vigente | — |
| Local-0045 | Los Componentes de Subsistema viajan como archivos, no transcriptos adentro de un markdown | Los Componentes de Subsistema que el Agente Multipropósito instala viajan como **archivos**, en la carpeta `base/` de la skill de instalación, con el mismo árbol que ocupan en el destino. Antes viajaban transcriptos adentro de `PLANTILLA.md`, que pesaba 310 KB de los cuales 204,8 eran copia byte a byte de un archivo ya existente. La premisa que lo sostenía —*el instalador corre donde solo llegó la carpeta del plugin*— era cierta pero no obligaba a transcribir: la carpeta del plugin lleva archivos, y `amp-actualizar.js` ya viajaba así. **La estructura reemplaza a toda lista**: no hay catálogo de qué instalar ni de qué comparar, y por eso desaparecen los tres defectos que las listas producían — el Componente que nadie agregó no viaja (`instalar-plugins-codex`), el que nadie agregó no se nivela (el nivelador cubría 11 de 74), y las dos copias se separan sin aviso. **Qué se hace con cada archivo lo declara él mismo en su frontmatter**, no una lista de excepciones: sin frontmatter u `origen: agente-multiproposito` se pisa entero; `origen: agente-desplegado` se pisa **solo hasta el separador de su tabla** y sus filas se preservan — arriba está la convención que manda el Agente Multipropósito y que hasta ahora quedaba vieja para siempre en cada repo instalado, abajo están las entradas de cada repo. Consecuencias: `ESTADOS.md` gana su par `ESTADOS-LOCAL.md` y `MOMENTOS`/`CLASES`/`ESTADOS` declaran su origen, con lo que la norma queda sin excepciones; `propagar-harness` **se retira** y lo reemplaza la Herramienta `sincronizar-base`; `lint-harness` cambia tres controles por cuatro que comparan archivo contra archivo en los dos sentidos. | 2026-07-30 | vigente | — |
| Local-0046 | Un encabezado solo se pisa si su tabla declara las columnas de la Base | **El nivelador pisa el encabezado de un registro `origen: agente-desplegado` solo cuando su tabla declara exactamente las columnas de la Base; si aparece una que la Base no trae, frena y decide el usuario, y si esa columna es lo único que difiere, no genera hallazgo (refina 0045, respeta 0042).** Pisar el encabezado de un registro al que el repo le sumó una columna —cosa que 0042 le permite— deja sus filas bajo una cabecera que ya no las describe. No se fusiona automáticamente porque una columna sumada por el repo y una renombrada por la Base dejan la misma evidencia. Detectado el 31/07/2026 a partir del reporte de un Agente Desplegado. | 2026-07-31 | vigente | [0046-encabezado-y-columnas.md](0046-encabezado-y-columnas.md) |
| Local-0047 | El código del Agente Multipropósito es Node sin dependencias externas | **Todo el código que el Agente Multipropósito distribuye —lints, hooks, Herramientas, el nivelador— se escribe en Node y usa solo su biblioteca nativa; no hay `package.json` ni `node_modules`.** El consumidor ya tiene Node sin haber tenido que elegirlo, porque Claude Code y Codex CLI corren sobre él; y una biblioteca nueva no la paga este repo, la paga cada repo que la instale. Era un supuesto arrastrado desde el primer script que ninguna de las 46 decisiones anteriores mencionaba. | 2026-07-31 | vigente | [0047-node-sin-dependencias.md](0047-node-sin-dependencias.md) |
| Local-0048 | Qué sube a la Base y qué se queda como documentación de este proyecto | **Un Componente de Subsistema sube a la Base —y viaja a cada Agente Desplegado— solo si le sirve para hacer bien su trabajo: le explica cómo funciona algo que va a usar, le evita perderse, o es una regla útil para cualquier propósito. Lo que este repo aprendió construyendo y propagando el Agente Multipropósito se queda acá como documentación del proyecto.** Vale para todos los subsistemas —conocimiento, preferencias, conducta, semántica, herramientas—, no solo para el que motivó la pregunta. El criterio distingue **por beneficiario, no por tema**: que el Agente Multipropósito instale aquello de lo que un Componente habla no lo vuelve útil a quien lo recibe ya hecho. Medido el 31/07/2026 sobre las quince páginas de conocimiento: ocho parecían universales por tema y solo tres pasan el criterio. | 2026-07-31 | vigente | [0048-que-sube-a-la-base.md](0048-que-sube-a-la-base.md) |
| Local-0049 | El código que comparten varios subsistemas vive en `.claude/common/` | **El código que necesitan dos o más subsistemas y no es de ninguno vive en `.claude/common/`**, no dentro de un subsistema ni en el registro de Herramientas: no se invoca, lo requiere otro código. Se escribe a mano en vez de tomar una biblioteca del ecosistema —que ya trae tapados defectos como el de la marca de orden de bytes— porque una dependencia nueva no la paga este repo sino cada Agente Desplegado que lo instale (Decisión Local-0047). El precio asumido: se elimina la duplicación, no la clase de defecto. Todo lo que vive ahí lleva su propia prueba, y **viaja o no según el Índice de Herramientas que lo declare** (modificado por Local-0050: antes viajaba todo, sin excepción). | 2026-08-01 | vigente | — |
| Local-0050 | Una Herramienta es maquinaria, la invoque quien la invoque | **Se amplía qué es una Herramienta: la maquinaria con que se construye y se mantiene el Producto, que no es parte del Producto. Quién la llama —el agente, u otro código del repo— no cambia lo que es.** Se suma el tipo `funcion` a `script`, `skill` y `MCP`, y con él entran al registro los módulos compartidos de `.claude/common/`, que hasta hoy eran el único código del repo que no figuraba en ningún índice. **Modifica Local-0007**, que había dejado los hooks afuera por "no invocable por el agente": ese criterio se retira, aunque los hooks y los controles de subsistema **siguen sin registrarse** —vienen con su subsistema por el Patrón, y pueden quedar así—; es una ubicación, no una negación de que sean maquinaria. La consecuencia práctica que motivó el cambio: el registro está partido por origen, así que **declarar una Herramienta es lo que decide si viaja**, y `common/` deja de tener su propia regla rígida ("todo lo de adentro viaja") que obligaba a mandarle a cada Agente Desplegado código que solo le sirve a quien publica. El control de `lint-harness` pasa a leer el Índice —localizándolo por el `origen` de su frontmatter, no por su nombre (Local-0042)— en vez de exigir a ciegas. **Descartado** dejarlo en una carpeta local aparte con una exclusión en `lint-herramientas`: resolvía el caso y dejaba el código igual de invisible, sumando una excepción en vez de sacar una. | 2026-08-01 | vigente | — |
| Local-0051 | El Buzón de Avisos Generales entrega en el turno lo que se averiguó en segundo plano | **Un trabajo que corre en segundo plano deja su aviso en `.claude/tmp/avisos/<origen>.txt`, y el hook repartidor `establecer-conducta` lo entrega en el momento `cada turno` junto a las reglas `Inyectar`, y lo borra: un aviso se da una vez por sesión y se regenera en el arranque siguiente mientras la condición persista.** Nace de un caso concreto: avisar que los plugins quedaron atrás. Ese dato no se puede averiguar al arrancar —el diagnóstico completo cuesta 1.657 ms medidos, de los cuales 618 ms por marketplace son la pregunta al remoto, y sin red se va al vencimiento del plazo, contra un presupuesto de 100 ms para un evento bloqueante— así que se averigua en segundo plano, sin que nadie lo espere, y hace falta un canal para decirlo cuando termina. Verificado el 02/08/2026 en Windows: un proceso lanzado con `detached` y `unref` sobrevive al hook que lo lanzó. **Descartado** entregarlo con un programa propio en `cada turno`: cuesta 48 ms medidos en cada mensaje, para siempre, por un aviso que aparece una vez cada varios días; el repartidor ya corre ahí y ya es Node, así que leer un archivo le cuesta lo que no arrancar un proceso. **Descartado** también entregarlo en el arranque siguiente por la Pantalla de bienvenida: es gratis, pero llega una sesión tarde. El buzón **no sabe nada de plugins**: un archivo por origen, y cualquier trabajo en segundo plano futuro escribe ahí. **El aviso pone primero lo que rompe** —una dependencia sin declarar o un plugin sin instalar dejan al plugin sin cargar y sus skills no existen en la sesión, sin ninguna señal— y recién después la versión nueva; dice que hay que **reiniciar** para tomarla, y que **informa, no actúa**, porque también entra al contexto del modelo. **La consulta al remoto se apaga** con `AMP_SIN_RED=1` en el bloque `env` del `settings.json` del repo: corre en cada arranque de cada Agente Desplegado, y una salida a internet que el usuario no pidió tiene que poder desactivarse. Se emite como `systemMessage` —lo ve el usuario, que es quien decide— y como `additionalContext`, para que el modelo pueda responder. Ante cualquier error se ignora: nunca rompe el turno. **No agrega una clase** a `CLASES.md`. | 2026-08-02 | vigente | — |
| Local-0052 | El control de terminología alcanza al código, avisando | **El momento `al escribir` amplía su condición de los `.md` a los archivos de código del repo, y ahí el control de terminología siempre avisa y nunca frena, cualquiera sea la columna `Control` de la fila (modifica la Decisión Local-0038, que había fijado la condición en «`.md` de cualquier parte del repo salvo `tmp/`»).** El razonamiento de entonces era que la terminología ajena entraba por el texto; medido el 02/08/2026, `juguete` —vetado por la fila Local-0041— entró nueve veces en un `.js`, viajó a `base/` rumbo a los Agentes Desplegados y ningún control lo tocó: lo marcó el lint, después. **En código nunca frena**, y no es prudencia sino imposibilidad: la exención que hace usable al control —se frena *usar* el término, no *nombrarlo*, mirando lo que queda fuera de las comillas simples invertidas— no se traduce a un `.js`, donde esas comillas son plantillas de cadena. Sin exención de cita, bloquear dejaría archivos inescribibles sin salida. En los `.md` no cambia nada. | 2026-08-02 | vigente | — |
| Local-0053 | La Base pública no incluye elecciones personales del autor | El Agente Multipropósito se publica para terceros con una Base que contiene el mecanismo general y las reglas necesarias para operarlo, pero no las preferencias, convenciones ni reglas de conducta que el autor eligió para su uso diario. Esas elecciones permanecen fuera de la Base y se reutilizan en otros Agentes Desplegados mediante un mecanismo separado. | 2026-08-03 | vigente | — |
| Local-0054 | Replicar una elección personal no la convierte en Base | Un Componente de Subsistema que se reutiliza en varios Agentes Desplegados se incorpora como Aprendizaje local de cada destino; reutilizarlo no lo convierte en parte de la Base pública. Solo una decisión separada, fundada en que sirve a cualquier propósito y usuario, puede promoverlo a la Base. | 2026-08-03 | vigente | — |
| Local-0055 | Copiar una entrada entre Agentes Desplegados es una operación puntual | Cuando el usuario pide llevar un Componente de Subsistema de un Agente Desplegado a otro, el subsistema destino lo incorpora como entrada local: verifica equivalencias y conflictos, asigna su propio Código, escribe sus archivos y corre su lint. Esto no crea un origen nuevo ni un vínculo permanente; los cambios posteriores de la fuente no se propagan automáticamente. | 2026-08-03 | vigente | — |
| Local-0056 | El catálogo de Recomendadas viaja en el Producto público sin instalarse | **El Agente Multipropósito publica un catálogo de Preferencias Recomendadas dentro del plugin, y una habilidad lo muestra para que el usuario del destino elija qué adoptar. Precisa el alcance de la Decisión Local-0053:** lo que la Base pública no puede hacer es *imponer* una elección del autor, no *ofrecerla*; ninguna recomendada se carga ni se instala sin elección explícita, y lo adoptado entra como Aprendizaje del Agente Desplegado con Código propio, sin vínculo con el catálogo. El catálogo es **derivado** del Índice del Agente Desplegado de quien publica, regenerado por `sincronizar-recomendadas`, para que la misma preferencia no quede escrita en dos lugares que divergen sin control. **Descartado** un conjunto exportable entre Agentes Desplegados: resolvía lo mismo pero exigía tener a mano el repo fuente, mientras que el catálogo llega con el plugin. | 2026-08-04 | vigente | — |
| Local-0057 | Contrato de planes de ocho estados en un solo eje | **Contrato de estados de planes de ocho estados en un solo eje (reemplaza Local-0005).** Un plan está en exactamente un estado: `Nuevo` (registrado, sin analizar), `Análisis` (se delimita, contrasta o ajusta), `Listo` (analizado y listo para ejecutar), `En curso` (ejecutándose), `En pausa` (interrumpido con intención de retomar), `Diferido` (pospuesto) y los terminales `Ejecutado` y `Descartado`. Se **conservan** de Local-0005 el eje único y la declaración configurable en `ESTADOS.md` que el lint lee; lo que **cambia** es que se reintroduce la granularidad diseño/ejecución que Local-0005 había colapsado. **Por qué ahora aporta y en 2026-07-18 no:** Local-0005 juzgó que la distinción diseño/ejecución no servía al flujo, y era cierto sin habilidades por verbo. La reingeniería (plan Local-0075) crea una familia por verbo —`analizar`, `ejecutar`, `pausar`, `retomar`…— donde cada estado tiene una habilidad que lo mueve y `Listo` es la compuerta entre analizar y ejecutar; ahí la granularidad deja de ser decorativa. **`En pausa` es el único estado con `estado_a_retomar`** (valor `Análisis` o `En curso`), dato transitorio que vive en el archivo del plan, no en `PLANES.md`; `Diferido` vuelve siempre a `Análisis`. El contrato completo —transiciones y datos obligatorios— vive en `ESTADOS.md`, fuente de verdad única. | 2026-08-05 | vigente | — |
| Local-0058 | Todo estado vivo llega a Descartado, y `estado_a_retomar` se deriva de las entradas a En pausa | **Un plan se puede abandonar desde cualquier estado vivo: `En pausa` y `Diferido` suman salida a `Descartado`, y `En pausa` suma además salida a `Diferido` (precisa Local-0057).** La restricción anterior no se decidió: el contrato de la reingeniería enumeraba destinos para `Nuevo`, `Análisis`, `Listo` y `En curso`, pero de `En pausa` y `Diferido` solo declaraba reglas *sobre la retomada* —«`Diferido` vuelve siempre a `Análisis`», que significa «al volver al trabajo no salta a ejecución», no «su único destino es `Análisis`»—; al bajar el contrato a la tabla legible por máquina, esas dos reglas se volvieron enumeración cerrada y prohibieron lo que nadie había prohibido. Lo confirma que la habilidad `diferir-plan` ya aceptaba `En pausa` como origen: la habilidad publicada contradecía la tabla publicada. **`Agente-Coordinador` ya lo había resuelto** en su decisión propia del 27/07/2026 —nueve días antes de Local-0057— definiendo `En pausa` como *espera de una decisión*, donde las cuatro salidas son las cuatro respuestas posibles a una consulta pendiente; esa definición cae dentro de la del contrato, así que incorporar las transiciones lo cubre sin que declare estados propios. **Consecuencia obligada:** los valores válidos de `estado_a_retomar` dejan de derivarse de la fila `En pausa` —que desde ahora incluye salidas de cierre, y habría dado por válido un `estado_a_retomar: Descartado` sin avisar— y pasan a derivarse de **las entradas** a `En pausa`: retomar es deshacer la pausa, así que el destino de retomada es por definición el origen de la pausa. Sigue vigente la única restricción con fundamento: `Diferido` no vuelve directo a `Listo` ni a `En curso`. | 2026-08-05 | vigente | — |
