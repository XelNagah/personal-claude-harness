# Inicializador de Repos Custom — jllarens

Propósito: autorar y distribuir el harness multipropósito (subsistemas + funcionalidades/plugins + marketplace).

<!-- PROVISIONAL. La Pantalla de bienvenida lee de acá el Título (H1) y el Propósito.
     El formato definitivo, la captura en el setup y el @import desde AGENTS.md los define
     el plan `Identidad del Agente - Titulo y Proposito persistidos`. Renombre a
     "Agentes Multi Propósito" pendiente del plan de namespacing. -->
